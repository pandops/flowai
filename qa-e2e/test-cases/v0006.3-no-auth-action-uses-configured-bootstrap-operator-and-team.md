# Test case: v0006.3-no-auth-action-uses-configured-bootstrap-operator-and-team

## Implements

`web-ui` — `### Requirement: Operator actions carry the selected team identifier`

## Intent

Verify that changing the selected team changes the team identifier used by a UI mutation.

## Preconditions

- Mocked proxy supplies two teams with separate State Registry launch parameters.

## Steps

1. Select `team-beta` in the browser.
2. Add an env variable through the UI.
3. Inspect proxy forwarding and both teams directly in State Registry.

## Expected outcome

The forwarded path contains `team-beta`; only its canonical definition changes.
No operator identity, Keycloak group, or membership proof is invented.

## Implementation reference

`qa-e2e/web-ui/tests/v0006-selected-team.spec.ts` — `v0006.3 mutations use the currently selected team id`.

RED: `npx playwright test tests/v0006-selected-team.spec.ts --timeout=10000` — UI mutation controls were absent. GREEN: `npx playwright test` — passed.
