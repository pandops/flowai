# Test case: v0006.6-invalid-configuration-rejected

## Implements

`mocked-proxy` — `### Requirement: Mocked proxy supplies an externally prepared team list`

## Intent

Verify deterministic empty, invalid, and multi-team fixture behavior without membership discovery.

## Preconditions

- Mocked proxy can be configured with empty, malformed, and valid team lists.

## Steps

1. Load the UI for each fixture and observe requests and selection state.

## Expected outcome

Empty produces a non-mutating empty state; malformed entries are rejected by
the fixture boundary; valid multiple entries render exactly and one is
selected. No Keycloak or membership call occurs.

## Implementation reference

`qa-e2e/web-ui/tests/v0006-mocked-proxy-rest.spec.ts` — `v0006.6 rejects malformed supplied teams and accepts an explicit multi-team list`. RED rejected missing validation; GREEN is included in the 24/24 Web UI suite.
