# Test case: v0006.11-response-remains-unchanged

## Implements

`mocked-proxy` — `### Requirement: Mocked proxy remains non-authoritative`

## Intent

Verify that integration-mode proxying does not become canonical storage or transform responses.

## Preconditions

- State Registry returns distinctive success and denial responses.

## Steps

1. Request both responses through the mocked proxy and perform a UI mutation.
2. Compare responses and inspect proxy storage plus State Registry.

## Expected outcome

Responses are unchanged, the mutation exists only in State Registry, and the
mocked proxy stores no canonical platform record.

## Implementation reference

`qa-e2e/web-ui/tests/v0006-mocked-proxy-rest.spec.ts` — `v0006.11 forwards State Registry status and body unchanged`. GREEN is included in the 24/24 Web UI suite.
