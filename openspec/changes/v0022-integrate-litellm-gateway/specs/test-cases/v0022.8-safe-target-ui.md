# Test case: v0022.8-safe-target-ui.md

## Implements

`web-ui` — `### Requirement: Web UI manages only safe FlowAI target references`.

## Intent

Verify team UI handles typed safe references and blocks incompatible choices without credentials.

## Preconditions

A team operator, model/A2A aliases, compatible and incompatible agent capabilities, and a secret canary exist.

## Steps

1. Register both target kinds through Web UI.
2. Inspect list/detail views and browser traffic.
3. Select an incompatible agent/target pair, then a compatible pair.

## Expected outcome

No browser artifact contains the canary; the incompatible submission is blocked; the compatible canonical same-team target is saved.

## Implementation reference
