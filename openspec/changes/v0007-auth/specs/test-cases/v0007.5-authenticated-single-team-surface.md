# Test case: v0007.5-authenticated-single-team-surface

## Implements

`web-ui` — `### Requirement: Web UI renders the configured-team operator surface`

## Intent

Happy-path test verifying the authenticated single-team UI with display-only canonical name and no membership features.

## Preconditions

- The authenticated session is bound to `team-alpha` with canonical `team_name=Alpha Team`.

## Steps

1. Sign in through the Web UI.
2. Open the dashboard and inspect visible team context and controls.

## Expected outcome

The dashboard displays `Alpha Team` for the token-bound team and provides no team selector, team switch, team CRUD, or membership-management control.

## Implementation reference

