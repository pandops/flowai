# Tasks

## 1. Implementation-start artifact transition

- [x] Move every `specs/test-cases/v0007.*.md` definition unchanged to `qa-e2e/test-cases/` before writing any runnable E2E test or production code; verify the source directory contains no `.md` files and destination IDs are not overwritten.
- [x] Create the self-contained `qa-e2e/api-gateway/` Playwright package and add it to `qa-e2e/scripts/run-all.mjs`; provide provider-neutral OIDC discovery/token/UserInfo/JWKS fixtures with per-endpoint outage and timeout controls, string-array and object-array claims, requested-scope observations, operator and admin JWT fixtures, token/session helpers, isolated Gateway/State Registry PostgreSQL schemas and roles, read-only exact-value database assertion clients, State Registry header/request spies, audit fixtures, and WebSocket event publishers; verify `npm --prefix qa-e2e/api-gateway run test:smoke` passes before the first v0007 RED run.
- [x] Validate `specs/api-gateway/openapi/auth.openapi.yaml` and `specs/state-registry/openapi/team-admin.openapi.yaml`, and generate contract assertions for every documented auth/admin operation, request schema, status, and safe error envelope before production handlers are written.

## 2. OIDC team and Gateway-owned mapping slice (`v0007.16`-`v0007.23`, `v0007.25`, `v0007.27`)

- [x] **RED:** Generate runnable API E2E tests at `qa-e2e/api-gateway/tests/v0007-oidc-team-resolution.spec.ts` from `v0007.16` through `v0007.23`, `v0007.25`, and `v0007.27`; run `npm --prefix qa-e2e/api-gateway test -- tests/v0007-oidc-team-resolution.spec.ts` and verify behavior-specific failures for provider-neutral claim parsing, OIDC state/nonce/token validation, callback XOR and provider errors, source/pointer/config validation, partial mapping, operator/admin endpoint outages/timeouts, freshness, response-specific error codes, configured-issuer enforcement, provider-neutral admin authentication, missing canonical teams, State Registry outage, idempotency, conflicts, identity-data exclusion, or database-role isolation.
- [x] **GREEN:** Implement OIDC authorization-code flow with PKCE; one configured claim source and RFC 6901 pointer; strict claim/scope/timeout/freshness configuration; API Gateway-owned encrypted sessions, mappings, observations and derived access; provider-neutral admin JWT validation; versioned auth/admin handlers from `auth.openapi.yaml`; atomic idempotent mapping registration; deterministic partial mapping; and fail-closed OIDC outages. Configure separate Gateway and State Registry schemas, roles, credentials, and migration histories even when both use one PostgreSQL instance; rerun the exact RED command and verify all cases pass.
- [x] **REFACTOR:** Separate OIDC protocol handling, Gateway mapping persistence, and State Registry canonical-team client without cross-schema access or browser membership authority; rerun the targeted file and related State Registry admin tests and keep both green.
- [x] **STAGE 1:** Assert exact Gateway and State Registry database values after each public E2E flow; validate mapping unique constraints, idempotent retry values, conflict rollback, membership observation times, session lifecycle, absence of plaintext provider credentials, and absence of identity/membership columns or values in State Registry. Inspect both services' migrations, schema-qualified queries, connection configuration, and runtime consumption paths; prove each reader/writer uses only its service-owned schema.
- [x] **STAGE 2:** Start both production images against one PostgreSQL instance with separate runtime roles. For each role, first run a positive sentinel query against its own schema, then require cross-schema `SELECT`, `INSERT`, `UPDATE`, and migration-DDL probes to fail with PostgreSQL permission errors. Exercise each service through its public API before and after the probes, restart both services, re-read their API-visible state, and verify cleanup. Record commands, role names, SQLSTATE values, and container/image identifiers without recording credentials.
- [x] Fill moved definitions `v0007.16`-`v0007.23`, `v0007.25`, and `v0007.27` with exact runnable references and RED/GREEN evidence.
- [x] **KEYCLOAK COMPATIBILITY:** Run exact image `quay.io/keycloak/keycloak:26.6.3` with an imported temporary realm; execute browser authorization-code with PKCE, exchange the code, validate discovery/JWKS/UserInfo and normalized team claims, then run the same identity through Gateway mapping, team selection, and working-token issue. Keep Keycloak-specific configuration entirely in E2E fixtures and fill `v0007.45` with exact evidence.

## 3. Canonical working-token and immutable-team slice (`v0007.1`-`v0007.4`, `v0007.24`)

- [x] **RED:** Generate runnable API E2E tests under `qa-e2e/api-gateway/tests/v0007-auth-token.spec.ts` from `v0007.1` through `v0007.4` and `v0007.24`; run `npm --prefix qa-e2e/api-gateway test -- tests/v0007-auth-token.spec.ts` and verify behavior-specific failures for algorithm/key/claim validation, stale team acceptance, same-team renewal, distinct `jti`, logout, browser credential exposure, or auth-state leakage—not fixture, syntax, or dependency failures.
- [x] **GREEN:** Implement asymmetric allowlisted working-token signing and validation with key ring, bounded skew, full canonical claims, unique `jti`, short expiry, server-side encrypted OIDC sessions, secure cookie, in-memory Web UI bearer, same-team renewal through new token issue, and logout; rerun the exact RED command and verify all token/session cases pass.
- [x] **REFACTOR:** Separate canonical identity resolution, token verification, and refresh invariants while preserving fail-closed behavior; rerun `npm --prefix qa-e2e/api-gateway test -- tests/v0007-auth-token.spec.ts` and the related auth suite and keep both green.
- [x] Fill moved definitions `v0007.1`-`v0007.4` and `v0007.24` with `qa-e2e/api-gateway/v0007-auth-token.spec.ts` and exact Playwright test titles; record RED failures and GREEN passes.

## 4. Authenticated resolved-team Web UI slice (`v0007.5`-`v0007.7`)

- [x] **RED:** Generate browser E2E tests under `qa-e2e/web-ui/tests/v0007-authenticated-team.spec.ts`; run `npm --prefix qa-e2e/web-ui test -- tests/v0007-authenticated-team.spec.ts` and verify behavior-specific failures for authenticated team display, absence of membership/team-selection UI, Gateway-only bearer use, or browser-originated internal headers.
- [x] **GREEN:** Implement the minimum Web UI login/session flow, resolved `{team_id, team_name}` list rendering, resolved-team-only selection, one-team bearer attachment only to API Gateway REST/WebSocket traffic, and absence of team CRUD/membership behavior; rerun the exact RED command and verify `v0007.5`-`v0007.7` pass.
- [x] **REFACTOR:** Consolidate authenticated client/session handling without adding another backend origin or authorization logic based on `team_name`; rerun the targeted file and related `qa-e2e/web-ui/` suite and keep both green.
- [x] Fill moved definitions `v0007.5`-`v0007.7` with exact implementation references and RED/GREEN evidence.

## 5. Authenticated REST context and isolation slice (`v0007.8`-`v0007.14`)

- [x] **RED:** Generate API E2E tests under `qa-e2e/api-gateway/tests/v0007-authenticated-rest.spec.ts` for no-auth rejection, spoofed-header replacement, REST team isolation, unchanged responses, missing/ambiguous/stale team rejection, browser bearer containment, and canonical audit attribution; run `npm --prefix qa-e2e/api-gateway test -- tests/v0007-authenticated-rest.spec.ts` and verify each case fails for its absent security behavior before REST proxy production edits.
- [x] **GREEN:** Implement the minimum REST auth middleware and proxy path that validates signature/issuer/audience/expiry and canonical team freshness, rejects unusable context before proxying, removes browser `Authorization` and all internal identity headers, injects canonical `X-FlowAI-Operator-ID`, stable `X-FlowAI-Team-ID`, optional display-only `X-FlowAI-Team-Name`, and Gateway-generated `X-FlowAI-Request-ID`, and leaves resource ownership/audit persistence to State Registry; rerun the exact RED command and verify all cases pass.
- [x] **REFACTOR:** Centralize safe context construction and credential scrubbing while keeping response status, headers, and body unchanged and avoiding Gateway business policy; rerun the targeted file and related `qa-e2e/api-gateway/` REST suite and keep both green.
- [x] Fill moved definitions `v0007.8`-`v0007.14` with exact implementation references and RED/GREEN evidence.

## 6. Authenticated WebSocket isolation slice (`v0007.15`, `v0007.26`, plus WebSocket variants from `v0007.2`, `v0007.8`, `v0007.11`-`v0007.13`)

- [x] **RED:** Generate WebSocket E2E tests under `qa-e2e/api-gateway/tests/v0007-authenticated-websocket.spec.ts`; run `npm --prefix qa-e2e/api-gateway test -- tests/v0007-authenticated-websocket.spec.ts` and verify behavior-specific failures for unauthenticated/invalid/stale upgrades, spoofed context, bearer forwarding, response-frame transformation, cross-team exposure, close at `exp`, expired reconnect, or renewed-token reconnect.
- [x] **GREEN:** Implement the WebSocket upgrade and State Registry subscription path using the same token/access validation and credential containment as REST, plus deterministic authentication-expired close at token `exp`; rerun the exact RED command and verify renewed reconnect receives only token-team replay and live frames unchanged.
- [x] **REFACTOR:** Reuse verified auth/context primitives across REST and WebSocket paths without coupling transport handling to State Registry ownership policy; rerun both v0007 REST and WebSocket E2E files and the related Gateway suite and keep all green.
- [x] Fill moved definitions `v0007.15-authenticated-websocket-team-isolation.md`, `v0007.26-websocket-auth-lifecycle.md`, and every definition whose implementation reference includes a WebSocket variant with exact file/test IDs and RED/GREEN evidence.

## 7. System-admin and canonical-team lifecycle slice (`v0007.28`-`v0007.44`)

- [x] **RED:** Generate runnable cross-service API/WebSocket E2E tests at `qa-e2e/api-gateway/tests/v0007-team-administration.spec.ts` from `v0007.28` through `v0007.44`; run `npm --prefix qa-e2e/api-gateway test -- tests/v0007-team-administration.spec.ts` and verify behavior-specific failures for external admin-role assignment/removal and token-age bounds, complete Gateway/State Registry admin startup and runtime JWT/JWKS trust, canonical-team point-read authorization/status/shape/outage and v0009-compatible credential-free Gateway transport, current Gateway presentation refresh, released-name reuse, partial/combined metadata updates, every unique-name writer race, update/archive, active-or-archived default-image/claim, and archive/pre-existing-claim commit orders for both team-owned and system-owned Executors, immutable ownership, image-resolution timing, linearizable archive/new-ingestion and archive/existing-dedupe races, idempotent archive, pending/claimed/terminal lifecycle continuity, rejected source-input `POST /v1/tasks` including dedupe retries, delete/unarchive absence, mapping created before or after archive, archived-team list/token/REST/WebSocket access, archived-state Web UI rendering, provider-neutral OCI digest validation, and isolated deterministic transaction coordination.
- [x] **GREEN:** Implement provider-neutral admin JWT validation on the Gateway and State Registry mutating/admin-user surfaces; fail-fast trust configuration; bounded admin-token age; canonical-team point read with authenticated admin and credential-free network-admitted Gateway branches; Gateway presentation refresh; atomic team metadata, released-name reuse, and unique-name writes; shared OpenAPI/server OCI digest-reference validation; row-serialized default-image resolution, idempotent archive, claim, and ingestion/dedupe; archived-team rejection of every `POST /v1/tasks`; preservation of existing task lifecycle; the disabled-by-default isolated E2E test-control listener and allowlisted one-shot barriers; and Web UI archived-state rendering. Rerun the exact RED command and require all seventeen cases to pass.
- [x] **REFACTOR:** Separate team lifecycle persistence, admin authentication, canonical presentation lookup, and task-ingestion policy without cross-schema access; rerun the targeted suite and related State Registry admin, ingestion, image-resolution, Gateway auth, REST, WebSocket, and Web UI suites and keep them green.
- [x] **STAGE 1:** Assert exact `teams`, resource ownership, claimed-task image resolution, audit, Gateway mapping/history, and absence-of-local-admin values through read-only database roles. Inspect migrations, constraints, route graphs, authorization middleware, archive transaction/fan-out, and every active-team consumer.
- [x] Validate both State Registry OpenAPI deltas with Redocly, generate the test-control client from `specs/state-registry/openapi/test-control.openapi.yaml`, and prove the generated client drives the RED concurrency suite without private in-process hooks.
- [x] **STAGE 2:** Run State Registry, Gateway, Web UI, both Executors, PostgreSQL, and provider-neutral identity fixtures in the temporary containers + k3d E2E deployment; perform update and archive through public APIs, prove archived-team UI/REST/WS reads still work, prove every archived-team `POST /v1/tasks` returns `409 team_archived` before acknowledgement—including retry of a pre-archive dedupe key with its existing row unchanged—prove existing task lifecycle and history survive restart, and record production image/container identifiers.
- [x] Fill moved definitions `v0007.28`-`v0007.44` with exact runnable references and behavior-specific RED/GREEN evidence.

## 8. Completion gates

- [x] Run the complete repository E2E suite rooted at `qa-e2e/`; verify token claims, single-team sessions, spoof resistance, REST isolation, WebSocket isolation, bearer containment, audit attribution, external admin provisioning, and canonical-team update/archive behavior pass with no skipped tests.
- [x] Verify Web UI has one API Gateway origin, displays only Gateway-resolved canonical teams, and has no team CRUD/membership UI; API Gateway obtains OIDC teams only from the provider, owns identity mapping and membership persistence, never forwards browser/OIDC credentials, and never uses `team_name` for authorization; State Registry contains no identity mapping and remains canonical-team, resource-ownership, and platform-audit authority.
- [x] Verify all three auth diagrams declare the sequence family and no new diagram family was introduced.
- [x] Run the repository's normal build and related unit/integration suites as supplementary checks; these do not replace required E2E RED/GREEN evidence.
- [x] Run `npx -y @fission-ai/openspec@1.5.0 validate v0007-auth --strict --no-interactive` after implementation artifacts and task evidence are complete.
