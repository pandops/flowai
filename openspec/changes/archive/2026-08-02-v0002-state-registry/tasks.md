# Tasks

Implementation SHALL proceed in the order below. No GREEN production edit may begin until its corresponding runnable E2E test has failed for the expected behavior-specific reason. Every slice SHALL record the exact RED command and expected failure, the exact GREEN command and pass, and the related-suite result before its implementation task is checked. REFACTOR may run only while the targeted and related tests remain green.

The change supersedes the earlier unimplemented v0002-draft atomic `approvals` endpoint (which was never accepted into `openspec/specs/` and is not a baseline State Registry endpoint) with FIFO `claim` (one `command_id` per claim, idempotent on `(task_id, command_id)`), removes ingestion-time `dispatched`, replaces OpenBao Transit with local AES-256-GCM, adds admin `/admin/*` registration for teams, source systems, and task types, exposes admin-only read-only `GET /admin/tags` and `GET /admin/tasks` projections, requires `task_type_id` (REQUIRED) and a server-derived `required_tag` at listener ingestion, requires `default_image` at team registration, declares the team resource create-only (no `PUT`/`PATCH`/`DELETE` team endpoint), globally unique `source_systems.listener_identity` so one listener principal maps to exactly one `(team_id, source_system_id)` pair, integrity-protected cursors bound to endpoint, scope, filter tuple, ordering, and page direction for both Gateway and admin collections, and codifies four-level image precedence. Continuity of historical test IDs (v0002.1 through v0002.59) is preserved; new behaviors land under contiguous v0002.60 through v0002.81 IDs without renumbering earlier files.

## 1. Implementation-start test activation and API-only harness

- [x] **RED PREPARATION:** preflight destinations, then move `specs/test-cases/v0002.1-*.md` through `v0002.81-*.md` unchanged into `autotest/test-cases/` as the first implementation mutation; verify all 81 immutable IDs (`v0002.1` through `v0002.81`) are contiguous, no destination collides, and no v0002 definition remains under the change folder.
- [x] **RED:** bootstrap `autotest/state-registry/` as an API-only Playwright suite that starts real State Registry, PostgreSQL, and a configured AES-256-GCM key; provides distinct listener, team-owned Executor, system-owned Executor, trusted API Gateway, and authenticated system-administrator identities bound to `team-a` or `team-b`; supports controlled Registry restarts; can observe decrypt operations; and can open concurrent WebSockets. Verify a smoke test fails only because the protected State Registry behavior is not implemented, not because fixtures, certificates, or dependencies are broken.
  - **RED evidence (final verification):** `npm --prefix autotest/state-registry test` reports `PASS (39) FAIL (1)`. The single failing test `infrastructure healthy then protected POST /admin/teams fails because the behavior is absent` produces:
    ```
    expect(received).toBe(expected) // Object.is equality
    Expected: 201
    Received: 404
    ```
    proving infrastructure is healthy (`/v1/livez` 200, `/v1/readyz` 200 with `dependencies.postgres=true` and `dependencies.aes_key=true`, decrypt-operation counter `0`) while the protected `POST /admin/teams` admin team-create endpoint is absent. The remaining 39 tests cover identity-fixture immutability, redact rules, container-runtime error sanitization, the websocket helper (including the AggregateError close/terminate failure path), the worker transactional cleanup with deterministic `processKillFailureCount`/`removeFailureCount` injection, the concurrent restart+teardown race, the AggregateError cleanup propagation contract, the restart contract (post-restart `baseUrl` reflects the live process), a compiled-binary regression that proves the fixture drives the binary directly (not via `go run`), and the `StartPostgresContainer` failure-injection contract (primary preserved; cleanup error aggregated with the primary). No tests are skipped. Go build: `Go build: Success`. Go race tests: `Go test: 28 passed in 5 packages`. TypeScript: `TypeScript: No errors found`. `npx @fission-ai/openspec@1.5.0 validate v0002-state-registry --strict` reports `Change 'v0002-state-registry' is valid`. Post-run cleanup inspection: zero `state-registry-pg-*` containers, zero `state-registry` Go processes.
- [x] **GREEN:** add one runnable Playwright test for every moved definition `v0002.1` through `v0002.81`; each title SHALL contain its immutable ID, every request SHALL use a supported external HTTP/WebSocket interface, and no test SHALL be skipped or replaced by an in-process assertion.
- [x] **GREEN VERIFY:** run `npm --prefix autotest/state-registry test -- --list` and require exactly one discoverable runnable test per `v0002.<ordinal>` from 1 through 81 with no gaps or duplicates; leave each definition's `## Implementation reference` blank until its exact file path and title are stable.
- [x] **REFACTOR:** centralize only team fixtures, transport identities, process lifecycle, HTTP/WebSocket clients, and response-shape assertions; rerun the harness smoke/list checks and require behavior assertions to remain explicit in each test.

## 2. Normalized team-owned PostgreSQL persistence

- [x] **RED (migration/integration):** add tagged PostgreSQL tests that fail because the exact 3NF team-owned schema, constraints, and foreign keys do not exist; the failure SHALL name missing tables or ownership constraints rather than an unavailable database fixture.
  - **RED evidence:** `go test -tags=integration ./svc/state-registry/... -run 'TestMigrations|TestTeamOwnedSchema|TestNormalizedRelationships|TestPersistenceConstraints|TestTaskOwnedEnvironmentForeignKey|TestAdminTables|TestImageColumns|TestRequiredTeamDefaultImage' -count=1 -v` reached healthy PostgreSQL and Goose version 1, then reported `0 passed, 8 failed`; failures named missing `teams`, `executors`, `tasks`, event, environment, admin, image, and ownership objects.
- [x] **GREEN:** add versioned State Registry migrations for exactly `teams`, `executors`, `tasks`, `task_events`, `executor_events`, `environment_definitions`, `secrets`, `secret_versions`, `audit_entries`, `task_control_requests`, `source_systems`, and `task_types`. `teams` SHALL own immutable `team_id`, REQUIRED opaque `default_image` (set at admin registration and immutable through any admin or operator path), and display-only `team_name`; authorization SHALL NOT depend on `team_name` or `default_image`.
- [x] **GREEN:** enforce 3NF relationships `teams 1:N executors` (team-owned only), `teams 1:N tasks`, `teams 1:N environment_definitions`, `teams 1:N secrets`, `teams 1:N source_systems`, `teams 1:N task_types`, `executors 1:N executor_events`, `executors 1:N tasks`, `tasks 1:N task_events`, `tasks 1:N task_control_requests`, `tasks 1:N environment_definitions` (task may be the parent of zero or more task-owned environment definitions in the same team), `source_systems 1:N tasks`, `task_types 1:N tasks`, `environment_definitions 1:N secrets`, and `secrets 1:N secret_versions`; child rows SHALL inherit and database-enforce the same team through composite foreign keys or equivalent constraints when `team_id` is non-null.
- [x] **GREEN:** enforce uniqueness on `(team_id, source_system_id, source_id)` and `(task_id, event_id)`; exactly one non-empty task tag and Executor tag; nullable `tasks.executor_id` and `tasks.owner_command_id` before claim and immutable after claim; `tasks.team_id` immutable for the row's lifetime; `tasks.ingested_at` set once at ingestion and immutable; `task_events.executor_id` non-null for the first `created` event emitted on claim and for every Executor-emitted event; `executor_events.executor_id NOT NULL` for every self event; `executor_events.team_id` non-null when `scope = team` and `NULL` when `scope = system`; `environment_definitions.task_id` nullable (NULL means team-wide, non-NULL means task-owned and SHALL reference a task with the same `team_id`); `tasks.image` nullable opaque image reference set at ingestion and immutable on later retries of the same `(team_id, source_system_id, source_id)`; `tasks.resolved_image` and `tasks.image_source` set atomically at claim and immutable thereafter; `teams.default_image` non-null opaque image reference created only at `POST /admin/teams` and never cleared. `secret_versions` SHALL reference logical `secrets` rows.
- [x] **GREEN VERIFY:** run `go test -tags=integration ./svc/state-registry/... -run 'TestMigrations|TestTeamOwnedSchema|TestNormalizedRelationships|TestPersistenceConstraints|TestTaskOwnedEnvironmentForeignKey|TestAdminTables|TestImageColumns|TestRequiredTeamDefaultImage'`; require the exact table set, 3NF placement, immutable team ownership, child-team rejection, nullability, uniqueness, same-team foreign keys, secret/version relationship, `environment_definitions.task_id` same-team constraint, image columns presence and nullability, `source_systems` and `task_types` ownership, and rollback behavior to pass. Historical tests `v0002.32`, `v0002.44`, `v0002.48`, `v0002.50` cover externally observable persistence, display-name authority, foreign task-parent rejection, and team-default-image visibility; new tests `v0002.60`, `v0002.61`, `v0002.62`, `v0002.63`, `v0002.64` cover admin endpoints, required team default, source/task-type image precedence, shared image, and source_id dedupe.
  - **GREEN evidence:** the exact tagged command reports `8 passed`; the complete tagged State Registry suite reports `36 passed`; `go test -race ./...` reports `141 passed in 27 packages`; `go build ./...` succeeds; the State Registry Playwright support suite remains at `39 passed, 1 failed`, with the sole expected RED still `POST /admin/teams` returning `404` instead of `201`.
- [x] **REFACTOR:** consolidate migration fixtures and SQL assertions without hiding table/constraint names or weakening exact-schema checks; rerun the same tagged command.

## 3. Admin `/admin/*` registration of teams, source systems, task types

- [x] **RED E2E:** implement `v0002.60`, `v0002.61`, `v0002.62`, and `v0002.63`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(60|61|62|63)\b'` and require behavior-specific failures for admin team create, source-system create, task-type create, and required `default_image`.
  - **RED evidence:** the exact Playwright command discovered all four immutable IDs and reported four behavior-specific failures while the protected routes were absent; the requests reached the healthy Registry/PostgreSQL harness but received `404` instead of the documented admin-create and Executor-registration-guard outcomes.
- [x] **RED unit/integration:** add tests for: caller without an authenticated system-administrator identity cannot create teams, source systems, or task types; team `default_image` is required and cannot be omitted; team `team_name` must be unique across teams; source-system `team_id` must reference an existing team; task-type `team_id` must reference an existing team and `execution_tag` is required; Executor registration body never creates or updates a team.
  - **RED evidence:** the untagged targeted Go command reported the expected build failure because `svc/state-registry/internal/platform` and the Section 3 admin symbols did not exist. The tagged Section 3a command reached PostgreSQL and passed two pre-existing schema invariants, then reported the same expected missing-platform build failure for the new handler/store contract tests.
- [x] **GREEN:** implement `POST /admin/teams`, `POST /admin/source-systems`, `POST /admin/task-types` accepting only an authenticated system-administrator identity. The Registry SHALL persist a generated immutable `team_id` together with the unique `team_name` and required `default_image`; persist a generated immutable `source_system_id` with the supplied `team_id` (which must reference an existing team), listener identity reference, and optional `default_image`; and persist a generated immutable `task_type_id` with the supplied `team_id` (existing team), required `execution_tag`, and optional `default_image`.
- [x] **GREEN:** reject all `/admin/*` calls without an authenticated system-administrator identity; reject team `default_image` omissions and duplicate `team_name`; reject source-system or task-type bodies whose `team_id` does not reference an existing team; persist nothing when any rejection fires. The Executor registration path SHALL never create or update a team.
- [x] **GREEN VERIFY:** rerun the targeted Playwright command and `go test ./svc/state-registry/... -run 'TestAdminCreateTeam|TestAdminCreateSourceSystem|TestAdminCreateTaskType|TestAdminRequiresDefaultImage|TestAdminRequiresUniqueTeamName|TestAdminSourceSystemReferencesTeam|TestAdminTaskTypeReferencesTeam|TestAdminEndpointsRejectNonAdmin|TestExecutorRegistrationDoesNotTouchTeam'`; require admin-only enforcement, immutable team_id generation, required `default_image`, no team creation/update through Executor or listener paths, and full audit-friendly admin endpoints.
  - **GREEN evidence:** targeted Playwright `v0002.60`-`v0002.63` reports `4 passed`, including duplicate/missing-field rejection, restart durability, same-team and cross-team listener-principal conflicts, unknown-team rejection, unauthenticated rejection, canonical response fields, row-count invariants, and the explicit Section 5 Executor-registration boundary. The exact Section 3 Go gate reports `36 passed`; the complete State Registry unit suite reports `83 passed`.
  - **Related verification:** `go test -tags=integration ./svc/state-registry/...` reports `98 passed`; `go test -race ./...` reports `196 passed in 29 packages`; `go build ./...` succeeds; TypeScript compilation succeeds; the Playwright support suite reports `40 passed`; strict change and full OpenSpec validation report `6 passed, 0 failed`; cleanup inspection finds zero `state-registry-pg-*` and zero `sr-pg-*` containers.
- [x] **REFACTOR:** isolate admin endpoint authorization and team-onboarding validation without exposing or duplicating the system-administrator authentication mechanism; rerun all admin tests.
  - **REFACTOR evidence:** temporary header-derived admin and Executor identities are confined to explicit test mode; production startup fails closed with no business routes until the mTLS identity slice lands. Unexpected repository causes are retained in structured logs while generic HTTP errors expose no database detail. Focused post-review HTTP tests report `11 passed`.

## 3a. Globally unique listener principal binding

- [x] **RED unit/integration:** add tests that prove the database-enforced global uniqueness index on `source_systems.listener_identity` rejects re-registration using the same listener identity reference under the same team, under a different team, and under any other variation; the uniqueness rejection SHALL prevent persistence and SHALL NOT create or update any other `source_systems` row. Add a migration-integration test that demonstrates the uniqueness index exists before any app code accepts the request.
  - **RED evidence:** the tagged Section 3a command passed the two already-present schema checks and failed to build the new admin contract tests because the platform/store/httpapi symbols were intentionally absent; the failure occurred after the PostgreSQL fixture was healthy.
- [x] **GREEN:** add a migration that enforces a global uniqueness index over `source_systems.listener_identity`; reject all `POST /admin/source-systems` calls whose `listener_identity` reference is already bound to any existing `(team_id, source_system_id)` row, regardless of the requested `team_id`. The refusal SHALL occur in the database transaction and SHALL NOT emit events, audit records, or partial `source_systems` rows.
- [x] **GREEN VERIFY:** rerun `v0002.61` via Playwright and `go test ./svc/state-registry/... -run 'TestSourceSystemListenerIdentityUniqueness|TestAdminSourceSystemRejectsDuplicateListenerIdentity|TestAdminSourceSystemRejectsCrossTeamListenerIdentity|TestSourceSystemListenerIdentityIndexExists'`; require the uniqueness index, the same-team rejection, the cross-team rejection, and the no-mutation persistence guarantee to pass.
  - **GREEN evidence:** the exact tagged Section 3a gate reports `8 passed`; v0002.61 passes with both same-team and cross-team duplicate `listener_identity` attempts returning `409`, one surviving canonical row, immutable team ownership, and no partial persistence.
- [x] **REFACTOR:** centralize listener-principal conflict classification at the repository boundary while preserving the database-enforced global uniqueness constraint and the no-partial-persistence transaction guarantee; rerun the Section 3a Go and Playwright gates.
  - **REFACTOR evidence:** PostgreSQL constraint `source_systems_listener_identity_key` is translated once to `ErrListenerIdentityConflict`, the HTTP boundary maps that sentinel to the documented `409 listener_identity_already_bound` response, and the exact tagged Section 3a and v0002.61 gates remain green.

## 3b. Opaque cursor integrity, scope binding, and reject-before-read

- [x] **RED unit/integration:** add tests that: produce a valid opaque cursor from a Gateway collection and a valid opaque cursor from an admin collection; tamper with the cursor byte stream and verify rejection; reuse a Gateway cursor against an admin endpoint and verify rejection; reuse a cursor under a different trusted Gateway `team_id` or different authenticated system-administrator role and verify rejection; reuse a cursor against a different endpoint and verify rejection; reuse a cursor whose filter tuple differs from the original request and verify rejection; reuse a cursor whose ordering differs from the original request and verify rejection; and reuse a cursor whose page direction differs (the contract permits forward-only cursors). The protected repository/DB-call counters SHALL record zero invocations for every rejected call before the underlying query runs.
  - **RED evidence:** with the admin list and Gateway list handlers stubbed to skip cursor validation, `go test ./svc/state-registry/internal/httpapi/... -run 'TestCursor|TestLimit|TestAdmin|TestGateway' -count=1 -v` reported `48 passed, 21 failed in 1 packages`. The 21 failures were behavior-specific: tampered envelope returned `status=200, want 400; body={"items":null,"page":{...,"count":0}}` (cursor silently accepted, repository silently called); cross-endpoint cursor reuse returned `status=200, want 400`; cross-team (team-a→team-b) cursor reuse returned `status=200, want 400`; cross-role (Gateway→admin) cursor reuse returned `status=200, want 400`; changed-filter cursor returned `status=200, want 400`; changed-ordering cursor returned `status=200, want 400`; wrong-direction (backward) cursor returned `status=200, want 400`; out-of-range limit values `0`, `201`, `-1`, `999` returned `status=200, want 400`; malformed limit values `""`, `" "`, `"abc"`, `"01"`, `"+1"`, `"1.5"`, `"50 "`, `" 50"`, `"0x1"` returned `status=200, want 400`. The recording repo recorded non-zero `ListTaskCalls()` for every rejected call, mechanically proving the protected query ran before validation. RED surface count: 21.
- [x] **RED RED unit:** add tests that the `limit` parameter rejects values below 1, above the documented maximum (200 for Gateway and admin collections), non-integer values, and malformed values BEFORE any protected repository call runs.
  - **RED evidence:** the same stubbed-handler run reported `TestLimitOutOfRangeRejectedBeforeQuery/0`, `TestLimitOutOfRangeRejectedBeforeQuery/201`, `TestLimitOutOfRangeRejectedBeforeQuery/-1`, `TestLimitOutOfRangeRejectedBeforeQuery/999`, `TestLimitMalformedRejectedBeforeQuery/raw=`, `raw= `, `raw=abc`, `raw=01`, `raw=+1`, `raw=1.5`, `raw=50 `, `raw= 50`, `raw=0x1` all failed with `status=200, want 400; body={"items":null,"page":{...}}`. The recording repo's `ListTaskCalls()` was non-zero for every malformed-limit case, mechanically proving the malformed limit reached the protected query.
- [x] **GREEN:** implement an opaque cursor encoder/decoder for every Gateway and admin collection that uses an HMAC under a server-controlled rotating `key_id` and bundles the endpoint, authenticated scope, complete filter tuple, deterministic resource-appropriate ordering, and forward-only page direction. Reject tampered, cross-endpoint, cross-scope, changed-filter, changed-ordering, or wrong-direction cursors with the same non-revealing `400 invalid_pagination` response shape before the underlying query runs. Reject `limit` values outside the documented range or that are malformed with `400 invalid_pagination` before any protected query runs.
  - **GREEN evidence:** the cursor package `internal/cursor/cursor.go` ships HMAC-SHA-256 over a constant header + `key_id` + envelope, base64url (no-padding) encoding, constant-time MAC verification via `hmac.Equal`, closed JSON envelope binding endpoint, scope (role-only for admin, team_id+operator_id for gateway), deterministic ordering, direction=forward, complete filter tuple, and the typed tag/task continuation tuples. `ParseLimit` rejects `0`, `201`, `-1`, `+1`, `01`, `1.5`, `0x1`, `0o1`, `" "`, `""`, multi-value via three-way `cursorParam` (absent | present | malformed). The admin list and Gateway list handlers validate cursor + limit BEFORE any `ListRepository` call; the rejection tests mechanically assert `listTaskCalls()==0`. The startup wiring reads `STATE_REGISTRY_CURSOR_KEY_HEX` and fails closed: when the key is absent the admin list and Gateway list handlers are NOT mounted, so production business routes remain closed until the cursor key is configured.
- [x] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./svc/state-registry/... -run 'TestCursorIntegrityRejectedBeforeQuery|TestCursorCrossEndpointRejected|TestCursorCrossScopeRejected|TestCursorChangedFilterRejected|TestCursorChangedOrderingRejected|TestCursorWrongDirectionRejected|TestLimitOutOfRangeRejectedBeforeQuery|TestLimitMalformedRejectedBeforeQuery|TestGatewayListTasksOrderingIngestedAtDesc|TestAdminTagsOrderingDeterministic|TestAdminTasksOrderingDeterministic'`; require that no protected repository/DB call runs for any rejected cursor or malformed limit, that the documented ordering is deterministic across pages, and that `GET /v1/tasks` returns `(ingested_at DESC, task_id DESC)` after the trusted `team_id` predicate.
  - **GREEN evidence:** the exact targeted Go command reports `24 passed in 9 packages`. After route-graph, authorization, review-finding, and boundary regression coverage was added, `go test ./svc/state-registry/... -count=1` reports `182 passed in 9 packages`; `go test -tags=integration ./svc/state-registry/... -count=1` reports `212 passed in 10 packages` (including no-overlap/no-skip walks, exact-limit final-page termination, claimed `timestamptz` scanning, full Gateway task projection, exact admin allowlists, and nullable/non-null JSON image fields); `go test -race ./svc/state-registry/... -count=1` reports `182 passed in 9 packages`; `go vet ./svc/state-registry/...` reports no issues; direct `tsc --noEmit --project autotest/state-registry/tsconfig.json` succeeds; `go build ./...` succeeds; the implemented Playwright contract slice (`v0002.60`-`v0002.63`, `v0002.79`, and the Section 3b cursor contract) reports `6 passed`; the Playwright support suite reports `41 passed`; `npx -y @fission-ai/openspec@1.5.0 validate v0002-state-registry --strict` reports `Change 'v0002-state-registry' is valid`; `npx -y @fission-ai/openspec@1.5.0 validate --all --strict` reports `Totals: 6 passed, 0 failed (6 items)`.
- [x] **REFACTOR:** centralize cursor envelope validation and collection route wiring while preserving endpoint/scope/filter/order/direction binding, reject-before-read behavior, and deterministic continuation tuples; rerun all cursor, list, route-graph, and authorization tests.
  - **REFACTOR evidence:** `internal/cursor` owns strict envelope decoding, bounded token validation, rotating active/previous key lookup, constant-time MAC verification, and limit parsing; list handlers reject oversized, unknown, or repeated scalar query parameters before repository access and translate only typed continuation tuples. PostgreSQL listing fetches `limit+1`, emits a continuation only when another row exists, scans nullable timestamps as typed times, and materializes one canonical full task row; the admin boundary narrows it to the exact 11-field allowlist while the Gateway boundary returns the full OpenAPI `TaskPage`. Startup returns keyring configuration failures before binding instead of silently mounting a partial API. `RoutesWithKeyring` mounts one `/admin` subtree and one `/v1` subtree, eliminating duplicate chi mounts. `TestRoutesWithKeyringMountsSection3bPaths`, the 182-test unit suite, the 212-test tagged integration suite, the 182-test race suite, and both Playwright gates remain green.

## 4. Team-bound listener ingestion and source_id deduplication

- [x] **RED E2E:** implement `v0002.2`, `v0002.33`, `v0002.65`, `v0002.66`, and `v0002.67`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(2|33|65|66|67)\b'` and require behavior-specific failures for listener identity/team authorization, no event at ingestion, `pending` projection, source_id-based dedupe with corrected cross-team independence wording, and cross-team dedupe independence through separately registered source systems.
  - **RED evidence:** before listener production code, the exact command discovered all five IDs. `v0002.2`, `v0002.33`, `v0002.65`, and `v0002.66` reached the healthy Registry and failed because `POST /v1/tasks` returned `405`; `v0002.67` reached the later Executor boundary and failed because Section 5 registration was absent. No harness or infrastructure failure masked the missing behavior.
- [x] **RED unit/integration:** add tests for: listener identity bound to one team and one source system; ingestion commits a `pending` row with no event and `ingested_at` set once; identical `(team_id, source_system_id, source_id)` retried within one team returns the existing task with no event and no state change; the same `source_id` MAY exist independently across teams only because each team registers its own source-system identity, never because one source-system identity is shared across teams; listener retries do not replace `image`; `task_type_id` is REQUIRED on listener ingestion; `required_tag` is derived server-side from the referenced task type's `execution_tag`; the listener is rejected when it submits a `task_type_id` belonging to another team or omits `task_type_id`; the Registry rejects any ingestion body that supplies a listener-authored `required_tag` field (whether at the top level, nested under a body wrapper, or as a request header) with `400 listener_supplied_required_tag_forbidden` and SHALL NOT silently ignore or silently overwrite that value; secret claim fails when an `admin_id` source-system identity is submitted against another team's `team_id`.
  - **RED evidence:** the untagged exact filter compiled and reported behavior-specific missing-route failures for every handler scenario. The tagged run reached PostgreSQL: eight schema pins were already green and three generated raw-SQL assertions failed around missing application orchestration. During implementation review, those three invalid schema-level assertions were replaced with production-repository coverage; handler tests use a recording repository and assert zero calls for every pre-persistence rejection. The runnable E2E RED above remains the production-code gate for this slice.
- [x] **GREEN:** implement listener ingestion with a service identity bound to one immutable authorized `team_id` and one immutable authorized `source_system_id`; require `task_type_id` referencing a registered task type whose `team_id` equals the authenticated listener's team; derive `required_tag` server-side from the referenced task type's `execution_tag`; reject any listener-supplied `required_tag` field with `400 listener_supplied_required_tag_forbidden` and never silently ignore or silently overwrite that value; verify the submitted `team_id`, `source_system_id`, and `task_type_id` against the authenticated listener; set `ingested_at` once; insert the complete team-owned canonical task in `current_state = pending` with no lifecycle event in one transaction; return the existing same-team task on `(team_id, source_system_id, source_id)` retry without resetting state or history.
  - **GREEN evidence:** `platform.TaskIngestionRequest`, the narrow `ListenerRepository`, transactional `Store.IngestTask`, strict listener handler, and test-mode-only route adapter now implement the slice. The store verifies the exact `(team_id, source_system_id, listener_identity)` binding, resolves the same-team task type, inserts with `ON CONFLICT DO NOTHING RETURNING`, and re-reads the canonical row without update. Unit, tagged integration, and race suites report `219`, `262`, and `219` passing tests respectively; `v0002.33` and `v0002.66` pass externally.
- [x] **GREEN:** reject listener submissions whose `team_id` does not equal the authenticated listener binding; reject source-system identifiers that don't belong to that listener's authorized team; reject duplicate-key submissions that violate the `(team_id, source_system_id, source_id)` uniqueness; reject submissions whose `task_type_id` is missing or references a task type belonging to another team; reject submissions that include a listener-authored `required_tag` field with `400 listener_supplied_required_tag_forbidden`.
  - **GREEN evidence:** handler tests reject missing/wrong listener roles, body/header binding mismatches, malformed or missing fields, and top-level, nested, or header-authored `required_tag` before repository access. PostgreSQL tests prove same-team dedupe, concurrent one-row convergence, immutable payload/image/`ingested_at`, distinct cross-team rows through separately registered source systems, opaque listener identities, foreign task-type collapse, and zero task events at ingestion. `go vet ./svc/state-registry/...`, direct TypeScript compilation, and `go build ./...` pass.
- [x] **GREEN VERIFY:** rerun the targeted Playwright command and `go test ./svc/state-registry/... -run 'TestListenerTeamBinding|TestListenerSourceSystemBinding|TestIngestPendingNoEvent|TestTeamScopedSourceIdDeduplicate|TestIngestSetsIngestedAt|TestCrossTeamSourceIdIndependenceViaSeparateSourceSystems|TestListenerRequiresTaskTypeId|TestListenerRejectsForeignTaskTypeId|TestRequiredTagDerivedFromTaskType|TestListenerCannotSupplyRequiredTag|TestListenerRejectsListenerAuthoredRequiredTag'`; require unauthorized-team, cross-team-source-system, missing-`task_type_id`, foreign-`task_type_id`, and listener-supplied-`required_tag` submissions to be rejected with `400 listener_supplied_required_tag_forbidden` (never silently ignored or silently overwritten); same-team retries to append no duplicate; different team submissions with different registered source systems to receive different tasks for the same `source_id`; restart reads to preserve ownership.
  - **GREEN evidence:** the exact Playwright command reports `5 passed`; the exact focused Go command reports `23 passed`; and focused same-team task-history boundary tests report `3 passed`, proving a newly ingested `pending` task exposes a non-nil empty event page while unknown and foreign task identifiers share the same non-revealing JSON `404`.
- [x] **REFACTOR:** simplify ingestion/repository boundaries while preserving one transaction, exact `pending` projection, REQUIRED `task_type_id`, server-derived `required_tag`, and the corrected cross-team semantics; rerun targeted and related ingestion tests.
  - **REFACTOR evidence:** ingestion remains behind the narrow `ListenerRepository` and one transactional `Store.IngestTask` operation; ordered event-history reading is isolated behind a separate read-only `TaskEventRepository`, so no lifecycle read concern widens or splits the ingestion transaction. The same `5 passed`, `23 passed`, and `3 passed` gates remain green.

## 5. One-tag Executor registration with team-owned or system-owned scope

- [x] **RED E2E:** implement `v0002.3`, `v0002.12`, `v0002.14`, `v0002.16`, `v0002.20`, `v0002.27`, `v0002.28`, `v0002.34`, `v0002.54`, `v0002.55`, `v0002.56`, and `v0002.59`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(3|12|14|16|20|27|28|34|54|55|56|59)\b'` and require failures for team-bound identity, exactly-one-tag registration, scope discriminator, same-team or cross-team FIFO discovery, foreign-task collection isolation, mTLS, capacity-independent discovery, or scope-change rejection.
  - **RED evidence:** the exact command reported `1 passed, 11 failed`. `v0002.12` already passed identity rejection; registration returned `501 registration_not_implemented` for team scope and `403 not_authorized` for system scope; discovery returned `404`; the concrete Executor exited before registration; and `v0002.20` reached the healthy process but failed because no HTTPS/mTLS listener was bound.
- [x] **RED unit/integration:** add table-driven registration tests for omitted/changed/foreign team when `scope = team`, omitted `team_id` when `scope = team`, supplied `team_id` when `scope = system`, `team_id` that does not reference an existing team (when `scope = team`), zero/one/multiple tags, and scope-change re-registration; add discovery query tests for eligibility-predicate-then-FIFO ordering: `team_id AND required_tag` (team-owned) and `required_tag` only (system-owned), unregistered tags, team filtering before pagination/counts/cursors for team-owned Executors, cross-team FIFO discovery for system-owned Executors, read-only behavior, and absence of capacity reads.
  - **RED evidence:** `go test ./svc/state-registry/internal/httpapi -run 'TestExecutorTeamRegistration|TestSystemExecutorRegistration|TestRegistrationTagCount|TestRegistrationScopeDiscriminator|TestTeamTagFifoDiscovery|TestSystemCrossTeamFifoDiscovery|TestDiscoveryShape|TestDiscoveryIgnoresCapacity' -count=1` reported `0 passed, 24 failed`; failures were the expected `501`, system-role `403`, and unmounted discovery `404`, not fixture or compilation failures. The PostgreSQL tests were added in the same RED slice and later exercised against the production route with the tagged gate below.
- [x] **GREEN:** implement Executor registration with `scope` discriminator; when `scope = team`, verify the submitted `team_id` matches the authenticated Executor service identity AND references an existing team and persist that team with one authorized tag, type, metadata, and observed `max_capacity`/`running_count`, rejecting omitted or changed teams and invalid tag counts without persistence; when `scope = system`, persist `team_id = NULL`, one authorized tag, type, metadata, and capacity observations, rejecting a non-null `team_id` and any invalid tag count; in both scopes reject a re-registration that names a different scope. Executor registration SHALL NOT create or update any team row.
- [x] **GREEN:** implement read-only discovery where the eligibility predicate is conditional on the Executor's scope. Team-owned discovery SHALL match `tasks.team_id = authenticated_executor.team_id AND required_tag = registered_tag` and SHALL return `204` or an empty collection when only foreign tasks match. System-owned discovery SHALL match `required_tag = registered_tag` only and SHALL return summaries for matching tasks across every team. In both scopes discovery SHALL order results `(ingested_at ASC, task_id ASC)` where `ingested_at` is immutable per task and `task_id` is the deterministic tie-breaker. Both predicates SHALL be applied before result shaping; no foreign count, cursor, page metadata, or empty/non-empty distinction SHALL leak; discovery SHALL never reserve, assign, or evaluate capacity.
  - **GREEN behavior evidence:** the eight-name Go gate reports `25 passed` including subtests; the focused tagged PostgreSQL gate reports `14 passed`; and Playwright IDs `v0002.3`, `.12`, `.14`, `.16`, `.20`, `.27`, `.28`, `.34`, `.54`, `.55`, `.56`, `.59`, plus the corrected `.63` regression pass. The concrete Executor validates the canonical registration response before becoming ready. `v0002.20` now launches the normal untagged binary and verifies production HTTPS/mTLS, WSS, certificate-derived admin/listener/Executor/Gateway authorization boundaries, and PostgreSQL `verify-full`.
- [x] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./svc/state-registry/... -run 'TestExecutorTeamRegistration|TestSystemExecutorRegistration|TestRegistrationTagCount|TestRegistrationScopeDiscriminator|TestTeamTagFifoDiscovery|TestSystemCrossTeamFifoDiscovery|TestDiscoveryShape|TestDiscoveryIgnoresCapacity'`; require immutable one-tag records, non-revealing foreign collection behavior for team-owned Executors, FIFO cross-team summaries for system-owned Executors, unchanged task state, and no capacity-based result.
  - **GREEN evidence:** the exact Section 5 Playwright command reports `12 passed`, including the production mTLS/WSS/PostgreSQL-chain contract; the exact focused Go command reports `25 passed`. Registration identity is bound to the authenticated Executor ID, team-owned discovery remains same-team, system-owned discovery crosses teams by exact tag, and no capacity observation gates results.
- [x] **REFACTOR:** isolate registration validation, scope discriminator, and eligibility/query construction without allowing caller-selected tenant predicates; rerun all registration/discovery tests.
  - **REFACTOR evidence:** registration validation remains isolated at the HTTP boundary and immutable scope/team enforcement plus eligibility query construction remain in the Executor repository; no caller-selected tenant predicate enters discovery. The same `12 passed` Playwright and `25 passed` Go gates remain green.

## 6. Atomic FIFO claim and immutable assignment

- [x] **RED E2E:** implement `v0002.68`, `v0002.69`, `v0002.70`, `v0002.71`, `v0002.72`, `v0002.73`, `v0002.4`, `v0002.21`, `v0002.22`, `v0002.28`, and `v0002.35`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(4|21|22|28|35|68|69|70|71|72|73)\b'` and require failures for same-team compare-and-set FIFO claim, stale conflicts, older-task rejection, capacity independence, foreign point-resource `404`, idempotent same-`command_id` retry, one command owning many tasks, and same-timestamp tie-break by `task_id`.
  - **RED evidence:** the exact Playwright command reports `2 passed, 9 failed`. Capacity-independent discovery and foreign/unknown point parity already pass; every claim-dependent case reaches the healthy Registry and receives route-not-found `404` instead of the expected `200`/`409`, while the FIFO tie-break case reaches the separately absent trusted-Gateway task point read. These are behavior-specific missing-surface failures, not fixture or authentication failures.
- [x] **RED unit/integration:** add concurrent claim and rollback tests for FIFO ordering by `(ingested_at ASC, task_id ASC)`, tag mismatch, team mismatch, saturated observations, immutable `owner_command_id` and `executor_id` after claim, `image_source` and `resolved_image` set once at claim, identical `(task_id, command_id)` retry, one `command_id` across multiple tasks, foreign/unknown not-found translation, and the absent `dispatched` semantics.
  - **RED evidence:** the production HTTP-bound claim tests reported `2 passed, 15 failed` before the route/store implementation; failures were missing-surface `404` or absent assignment behavior. PostgreSQL integration tests reached a healthy migrated database and failed against the absent `Store.ClaimTask` behavior, not fixture setup.
- [x] **GREEN:** implement `POST /v1/executors/{executor_id}/claim` carrying `task_id` and `command_id`. Inside one transaction the claim SHALL verify the task is `pending`, the task's required tag equals the Executor's single registered tag, the eligibility predicate from scope holds, and the requested task IS the oldest currently eligible `pending` task for that authenticated Executor; then atomically set immutable `tasks.owner_command_id` (from request), immutable `tasks.executor_id`, persist `tasks.resolved_image` and `tasks.image_source` from the four-level precedence, set `tasks.claimed_at`, append the FIRST lifecycle event `created` with non-null `executor_id = claiming_executor` and a payload meaning `task <task_id> loaded by <executor_id>`, project the task to `created`, and remove the task from this Executor's scope of discovery. Never read or evaluate capacity.
  - **GREEN evidence:** `Store.ClaimTask` locks the authenticated Executor and requested task, verifies scope/tag/FIFO eligibility, resolves image precedence, writes assignment/claim/image/projection, and appends the first `created` event in one PostgreSQL transaction. The HTTP claim suite reports `17 passed` and focused PostgreSQL claim tests report `26 passed`.
- [x] **GREEN:** return `200 claimed` to the unique winner and exactly one of `409 older_task_must_be_claimed_first` (when an older eligible task exists) or `409 task_already_claimed` (when a different `command_id` already claimed) or `404` (unknown, foreign, or non-`pending`) without any event append or assignment change. The first lifecycle event is `created`; no `dispatched` event exists or is appended at any time. Same `(task_id, command_id)` retry by the same Executor SHALL return the original `200 claimed` body with no new event. Multiple distinct tasks may be claimed by the same `command_id`; each task records that `command_id` exactly once and never changes.
  - **GREEN evidence:** v0002.4/.21/.22/.28/.35/.68-.73 report `11 passed`, proving unique concurrent winner, exact conflict codes, non-revealing foreign/unknown parity, same-command idempotency, one command across tasks, deterministic equal-time task-id tie-break, and persisted image metadata.
- [x] **GREEN VERIFY:** rerun targeted Playwright tests and `go test -tags=integration ./svc/state-registry/... -run 'TestFifoClaim|TestClaimTransaction|TestForeignClaimNotFound|TestClaimIgnoresCapacity|TestSameCommandRetry|TestOneCommandMultipleTasks|TestOlderTaskConflict|TestConcurrentFifoClaim|TestResolvedImagePersistedAtClaim|TestFirstCreatedEventOnClaim|TestPendingHasNoEvent'`; require one immutable assignee, one immutable `owner_command_id`, one `created` event, no foreign-team signal, no capacity gate, and no `dispatched` event ever appended.
  - **GREEN evidence:** the exact Playwright slice reports `11 passed`; focused tagged claim integration reports `26 passed`; and the complete State Registry Go suite reports `367 passed`.
- [x] **REFACTOR:** extract claim eligibility and FIFO error translation without splitting assignment, image resolution, event append, projection, and discovery removal across transactions; rerun all claim tests.
  - **REFACTOR evidence:** FIFO probing now propagates database errors instead of treating them as no older task; task point reads use a narrow `TaskPointReadRepository`; image resolution and idempotent-response reconstruction are isolated helpers while all claim mutations remain in one transaction. The `11`, `26`, and `367` test gates remain green.

## 7. Team-scoped lifecycle events, self events, projection, and strict ordering

- [x] **RED E2E:** implement `v0002.5`, `v0002.6`, `v0002.23` through `v0002.26`, `v0002.29`, `v0002.31`, `v0002.36`, and `v0002.30`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(5|6|23|24|25|26|29|30|31|36)\b'` and require failures for assignment ownership, team isolation, event/self-event separation, strict ordering, idempotency, invalid transitions, foreign point `404`, pending-before-claim, and restart recovery.
  - **RED evidence:** the exact Playwright command reports `2 passed, 8 failed`. Registry-created `created` and assignment restart durability already pass; task-event POSTs return unimplemented-method `405`, self-event POSTs return route-not-found `404`, and ordered histories remain empty, producing behavior-specific failures for the absent Section 7 write surfaces.
- [x] **RED unit:** add table-driven transition tests for `created -> running -> finished | failed`, running-before-created, terminal-before-running, repeated terminal, re-approval, unassigned same-team writer, foreign-team writer, identical retry, equal timestamp with greater/lower `event_id`, older tuple, and caller-supplied `accepted_sequence`; require behavior-specific failures before validators exist.
  - **RED evidence:** the focused lifecycle suite was introduced against the Section 6 route surface, where task-event writes returned `405` and Executor self-event writes returned `404`; the cases pin transitions, ownership, retry, tuple ordering, and explicit `accepted_sequence` rejection before the write handlers existed.
- [x] **RED integration:** force failures between event append and projection update and between `executor_events` append and observation mirroring; require no partial commit and test database rejection of foreign-team child ownership and null Executor ownership.
  - **RED evidence:** schema-level review exposed that the original foreign keys admitted a team-owned task event naming a foreign-team Executor and a team-owned self event naming a foreign-team task. PostgreSQL fault-trigger tests were added for both transaction boundaries, and migration assertions now require rejection of those foreign children and null task-event Executor ownership.
- [x] **GREEN:** accept `running`, `finished`, and `failed` only from authenticated `tasks.executor_id` with the same immutable team; require `event_id`, `task_id`, non-null `executor_id`, explicit envelope `team_id`, `event_type`, `occurred_at`, and `payload`; verify the envelope `team_id` equals the authenticated Executor team (team-owned) or equals the parent task's `team_id` (system-owned); append once by `(task_id, event_id)` and project state in the same transaction. Foreign task or Executor point identifiers SHALL return unknown-shape `404`; unassigned same-team writers SHALL append nothing; envelope `team_id` mismatches SHALL be rejected without append.
  - **GREEN evidence:** production handlers and PostgreSQL transactions enforce assigned-Executor ownership, scope-conditional team envelopes, immutable append plus projection, non-revealing point failures, and no mutation for unassigned or mismatched writers. The exact Section 7 Playwright slice reports `10 passed`.
- [x] **GREEN:** resolve an identical `event_id` retry before ordering checks; require each fresh `(occurred_at, event_id)` tuple to be strictly greater than the latest accepted tuple; read by `(occurred_at ASC, event_id ASC)`; allow equal timestamps only with greater monotonic `event_id`; reject `accepted_sequence` and every recovery override without append or projection change. The first lifecycle event `created` is emitted at successful claim with non-null `executor_id` and payload `task <task_id> loaded by <executor_id>`; no event was appended at ingestion when the task was `pending`.
  - **GREEN evidence:** retries compare event type, timestamp, and semantic JSON payload before returning the original row; changed retries conflict, fresh tuples use strict `(occurred_at, event_id)` ordering, histories return the persisted `event_id`, and raw-body override detection rejects `accepted_sequence` before repository mutation.
- [x] **GREEN:** append Executor lifecycle/health/capacity events separately with `executor_events.executor_id NOT NULL`, envelope `team_id` equal to the authenticated Executor team (team-owned) or null (system-owned), inherited same-team ownership or `NULL` ownership, and same-transaction mirroring of latest observations; never use those observations in discovery, claim, or lifecycle decisions.
  - **GREEN evidence:** Executor self events use a separate table and route, database triggers enforce scope and optional task ownership, and capacity observations mirror inside the append transaction without entering discovery or claim predicates.
- [x] **GREEN VERIFY:** rerun the targeted Playwright command, `go test ./svc/state-registry/... -run 'TestTransition|TestTeamEventOwnership|TestEventIdempotency|TestStrictEventOrdering|TestExecutorSelfEvent'`, and tagged transaction tests; require exact ordered histories, unchanged state on every rejection, no duplicate identifiers, no accepted sequence, no `dispatched` event ever appended, and no cross-team append.
  - **GREEN evidence:** exact Playwright reports `10 passed`; focused lifecycle Go reports `18 passed`; focused PostgreSQL rollback/retry reports `4 passed`; complete default Go reports `385 passed`; complete tagged PostgreSQL reports `464 passed`; migration integrity reports `1 passed`.
- [x] **REFACTOR:** consolidate transition, envelope, ownership, and ordering validation while preserving distinct task/self streams and transaction boundaries; rerun all lifecycle tests.
  - **REFACTOR evidence:** task and self-event repositories retain distinct interfaces while sharing semantic JSON retry comparison and nullable ownership comparison; task append precedes projection in one transaction, self append precedes observation mirroring in one transaction, and all Section 7 verification gates remain green.

## 8. Trusted Gateway context, team-filtered reads, controls, audit, and WebSocket

- [x] **RED E2E:** implement `v0002.7`, `v0002.12`, `v0002.37`, `v0002.41`, `v0002.42`, `v0002.43`, and `v0002.44`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(7|12|37|41|42|43|44)\b'` and require failures for trusted Gateway enforcement, operator team checking, controls, pre-shape filtering, plaintext-free audit, WebSocket isolation, or display-name non-authority.
  - **RED evidence:** the exact Playwright slice reports `4 passed, 3 failed`. Service-identity enforcement, trusted verified Gateway context, pre-shape team filtering, and display-only `team_name` already pass; both control scenarios return route-not-found `404`, and team-bound WebSocket connections receive zero Registry frames.
- [x] **RED unit/integration:** add tests that accept only authenticated Gateway service identity plus verified `operator_id`, immutable `team_id`, and `request_id`; allow the optional display-only `team_name` to be present or absent without affecting authorization; reject copied direct headers, invalid/multi-team operator context, foreign point identifiers, and team filters applied after pagination/count/cursor/aggregation. Add concurrent replay/live WebSocket isolation tests.
  - **RED evidence:** the exact Section 8 Playwright slice reported `4 passed, 3 failed`: controls returned route-not-found `404` and concurrent team-bound sockets received zero live frames. PostgreSQL tests now pin foreign no-mutation, control/audit rollback, assigned-Executor visibility, audit isolation, and team-filter-before-shape behavior.
- [x] **GREEN:** implement Gateway-only reads, subscriptions, controls, environment/secret writes, and audits. State Registry SHALL authorize solely by canonical `team_id`; the optional `team_name` SHALL remain display/audit metadata when present and its absence SHALL NOT alter authority, and changing or spoofing it SHALL NOT alter authority. Untrusted headers or payload tenant fields SHALL grant nothing.
  - **GREEN evidence:** one shared trusted-Gateway context binds verified `team_id` and `operator_id`; audited mutations additionally require the Gateway `request_id`. Task, event, Executor-event, control, audit, and WebSocket surfaces consume that context, while `team_name` remains unused for authority. Section 9 resource writes will consume the same boundary.
- [x] **GREEN:** apply `team_id` before pagination, cursor creation, totals, counts, grouping, aggregation, serialization, replay selection, subscription filters, live fan-out, and frame serialization for tasks, Executors, events, controls, environments, secrets, versions, source systems, task types, and audits. Every foreign point identifier SHALL use the unknown-resource `404` shape; no collection metadata or WebSocket frame SHALL signal foreign rows.
  - **GREEN evidence:** task, control, audit, event-history, and stream repositories place `team_id` in SQL before ordering or shaping. Audit cursors are HMAC-bound to endpoint, Gateway team/operator, full filter tuple, ordering, and direction; foreign control/audit/task points return unknown-resource `404`; concurrent sockets receive only their bound team's events.
- [x] **GREEN:** store same-team controls idempotently by `(team_id, task_id, operator_id, action)` with plaintext-free audit entries containing team, actor, action, resource, request, outcome, and timestamp; expose pending controls only to the assigned Executor (with the appropriate scope-conditional team predicate).
  - **GREEN evidence:** one transaction locks the same-team task, returns exact idempotent retries, appends one identifier-only audit row, persists one linked control, and rolls both back on failure. Pending-control reads require the immutable assigned Executor and apply team scope conditionally for team/system Executors.
- [x] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./svc/state-registry/... -run 'TestTrustedGatewayContext|TestForeignResourceNotFound|TestTeamFilterBeforeShape|TestTeamControl|TestTeamAudit|TestWebSocketTeamIsolation|TestTeamNameDisplayOnly'`; require response equivalence with foreign rows removed and zero cross-team frame, count, cursor, audit, timing, or existence signal.
  - **GREEN evidence:** exact Section 8 Playwright reports `7 passed`; affected unit packages report `264 passed`; focused PostgreSQL reports `7 passed`; complete default State Registry reports `385 passed`; complete tagged PostgreSQL reports `471 passed`.
- [x] **REFACTOR:** centralize trusted context parsing, mandatory team predicates, non-revealing not-found translation, and WebSocket binding without permitting optional tenant filters; rerun all Gateway/read/control/subscription tests.
  - **REFACTOR evidence:** `gateway_context.go` centralizes immutable Gateway role/team/operator binding with request-ID enforcement for audited routes; every operator handler consumes the bound context. Control/audit repositories retain narrow interfaces, and WebSocket reader/writer lifecycles remain cancellable with team filtering performed in SQL.

## 9. Team-owned environments, logical secrets, immutable versions, and open

- [x] **RED E2E:** implement `v0002.8` through `v0002.11`, `v0002.19`, `v0002.32`, `v0002.38` through `v0002.40`, `v0002.45`, `v0002.46`, `v0002.47`, `v0002.48`, `v0002.49`, `v0002.74`, `v0002.75`, `v0002.76`, `v0002.77`, and `v0002.78`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(8|9|10|11|19|32|38|39|40|45|46|47|48|49|74|75|76|77|78)\b'` and require failures for team-owned environment/secret reads or writes, task-owned environment/secret reads or writes, foreign task-parent rejection, parent-task-only open, logical secret versions, non-revealing foreign identifiers, complete token claims, scope-token algorithm/key_id/issued_at/expiry checks, terminal/unassignment replay denial, assignment, audit, restart durability, AES-256-GCM startup fail-closed behavior, AES-256-GCM success path, AES-256-GCM no-decrypt-on-denial, and `key_version` rotation metadata.
  - **RED evidence:** the exact 19-case Playwright slice reports `5 passed, 14 failed`. Restart durability, AES-key startup fail-closed behavior, and image-precedence cases already pass; every environment, logical-secret, scope-token, and open-environment scenario fails at the absent `POST /v1/environments` route with `404`, including the required non-revealing environment error body.
- [x] **RED unit/integration:** add tests for same-team environment CRUD, project/task applicability within a team, task-owned environment rows with parent `task_id` constrained to the same team, foreign `task_id` parent rejection returning `404 environment_unknown_or_unavailable`, logical `secrets` plus immutable referenced `secret_versions`, secret inheritance of parent environment scope, foreign environment/secret/version point resources, no decrypt operation before authorization, plaintext exclusion, AES-256-GCM nonce/tag/associated-data binding, AES-256-GCM rotation compatibility, bounded retries, startup fail-closed when the configuration-provided 256-bit key is missing, AES-256-GCM success path including plaintext in memory only, and decrypt-operation denial on every invalid scope-token condition with zero decrypt operations.
  - **RED evidence:** the exact 19-case E2E RED above established the absent environment route; the generated Go/PostgreSQL suite then pinned team-wide, task-owned, project/task applicability, secret-version immutability, AES-GCM, scope-token, plaintext-exclusion, and zero-decrypt denial behavior before the production repositories and handlers were completed.
- [x] **GREEN:** implement environment-definition writes only from trusted Gateway context for the same immutable `team_id`, storing non-secret values, optional `parent_task_id` (nullable; when set the row is task-owned and SHALL reference a task with the same `team_id`), and optional project/task applicability plus plaintext-free audit. Operators SHALL register and manage environments only for their team; task-owned rows SHALL be readable only by the parent task in the same team; scopes SHALL narrow use inside the team and never grant cross-team authority.
- [x] **GREEN:** implement same-team logical `secrets` rows associated with same-team environments and append immutable encrypted `secret_versions` rows referencing them. A secret under a task-owned environment SHALL be task-owned too and only the parent task in the same team may open it. Encrypt every submitted secret value with local AES-256-GCM authenticated encryption (256-bit data key, fresh random 96-bit nonce, 128-bit authentication tag, authenticated associated data binding team identifier, logical secret identifier, and version); persist the ciphertext together with the opaque provider-neutral `key_id` and `key_version` envelope; verify all team and scope ownership and the canonical task assignment before any decrypt operation; never persist, log, audit, or return plaintext, nonce bytes, ciphertext bytes, tag bytes, key material, or associated-data values beyond identifier-level metadata; foreign environment, secret, or version identifiers SHALL return unknown-shape `404` and perform no decrypt operation.
- [x] **GREEN:** implement `GET /v1/environments/{environment_id}/open` with the compact three-part signed scope token `<header>.<payload>.<signature>` carried in the `X-FlowAI-Scope-Token` header only; no scope-token fields appear in the request body, and the `task_id` query parameter is required. State Registry SHALL be the sole issuer and verifier of these tokens, signing them with an allow-listed HMAC algorithm from the set `HS256`/`HS384`/`HS512` (the "HMAC-SHA-256 or a stronger HMAC" family) under a server-controlled rotating key identified by a `key_id` claim inside the documented active key window. The protected-header `kid` SHALL equal the payload `key_id`; `issued_at <= server_now + 30 seconds`, `expiry > issued_at`, and `expiry - issued_at <= 5 minutes`. Verify the protected-header `kid` equals the payload `key_id` before any MAC computation, recompute the signature under the declared allow-listed HMAC algorithm and compare it with a constant-time comparison before any other check, then canonical claim shape and values (including the project-scope rule for `project_id`), `key_id` window, `issued_at`/`expiry` window, expected literal `audience`, every claim, the authenticated Executor team, same-team assignment, task-parent applicability (a task-owned environment SHALL be openable only by the parent task), project/task applicability, and non-terminal task state before any decrypt operation. Reject every invalid or unavailable open attempt — including missing or empty token, tampered MAC, algorithm outside the allow-listed HMAC set, protected-header `kid` not equal to payload `key_id`, `key_id` outside the documented active key window, lifetime over five minutes, expired or premature issuance, wrong audience, missing or mismatched claim (including a null `project_id` for a project-scoped environment), wrong Executor identity, terminal task state, and a foreign or unknown environment or task identifier — with the same non-revealing `404 environment_unknown_or_unavailable` response and zero decrypt operations.
- [x] **GREEN VERIFY:** rerun targeted Playwright tests and `go test -tags=integration ./svc/state-registry/... -run 'TestTeamEnvironment|TestTaskOwnedEnvironment|TestTaskOwnedSecret|TestTeamSecret|TestSecretVersions|TestOpenEnvironmentClaims|TestScopeTokenSecurity|TestAesGcm|TestStartupFailClosed|TestDecryptFailClosedOnBadAssociatedData|TestNoDecryptOnScopeTokenDenial|TestKeyVersionRotationMetadata|TestPlaintextExclusion'`; require same-team success for both team-wide and task-owned rows, parent-task-only open for task-owned rows, point-resource `404 environment_unknown_or_unavailable` parity for every invalid or unavailable variant (including foreign task parent on write and non-parent task on read), zero decrypt operations on denied writes/opens, immutable versions with key rotation metadata, AES-256-GCM fail-closed startup and decryption behavior, plaintext never in logs, audit entries, or error responses, constant-time MAC comparison, server-controlled `key_id` rotation, lifetime bounded to five minutes, and zero decrypt operations for every invalid or unavailable variant including terminal and not-assigned cases.
  - **GREEN evidence:** the exact Section 9 Playwright command reports `19 passed`; complete tagged State Registry reports `620 passed`; complete default State Registry reports `440 passed`; the independent `task_id` / `parent_task_id` regression and strict previous-key parser tests are included.
- [x] **REFACTOR:** separate team authorization, audit, scope-token verification, and AES-256-GCM concerns behind narrow State Registry-owned interfaces; rerun all environment/secret/open tests.
  - **REFACTOR evidence:** environment, secret, scope-token, keyring, and local AES-GCM responsibilities remain separated behind Store-owned interfaces; parser and applicability corrections retain the same public wire contract and all Section 9 gates remain green.

## 10. Docker Executor client, team identity, and local capacity ownership

- [x] **RED E2E:** implement `v0002.13` through `v0002.19`, `v0002.68`, `v0002.69`, `v0002.70`, `v0002.71`, `v0002.72`, `v0002.73` against the finalized team contract; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(13|14|15|16|17|18|19|68|69|70|71|72|73)\b'` and require failures because the Docker Executor lacks one-team registration, same-team FIFO discovery, FIFO claim with `command_id`, strict event ordering, foreign `404` handling, complete scope-token checks, local-only capacity enforcement, or removed local image fallback.
  - **RED evidence:** before the Registry runtime path existed, `v0002.19` observed the binary repeatedly call legacy `GET /v1/tasks?filter=openhands` and receive `403 not_authorized`; during GREEN the exact slice narrowed through `8 passed, 5 failed`, with remaining failures on container lifecycle/labels and the absent finalized environment-open behavior rather than harness startup.
- [x] **RED unit/integration:** add Docker Executor tests for one immutable team + one tag, local slot gating, same-team exact-tag FIFO discovery, FIFO claim with `command_id`, claim-before-start, `409 older_task_must_be_claimed_first` and `409 task_already_claimed` recovery, foreign/unknown `404` equivalence, non-null event ownership, strictly increasing ordering tuples, no `accepted_sequence`, stable retries, same-team controls, all scope-token claims, display-name non-authority, terminal slot release, and verified use of `tasks.resolved_image` with no local fallback substitution.
  - **RED evidence:** focused client/state tests were added around typed HTTP errors, claim-before-start, local slots, event envelopes, token opening, and resolved-image adoption before the corresponding v0002 runtime methods were completed; the existing E2E RED above additionally exercised the real process boundary.
- [x] **GREEN:** update the self-contained Docker Executor State Registry client and OpenAPI documents for one-team or system-scoped registration, same-team exact-tag FIFO discovery, FIFO claim with `command_id`, task/self events, assigned controls, and team-bound open-environment reads. Every Executor-emitted task and self event envelope SHALL include `task_id` (when task-scoped), `executor_id`, explicit `team_id` (executor binding for team-owned, parent-task team for system-owned), `event_id`, `event_type`, `occurred_at`, and `payload`. Remove Router and Env Registry client use from the v0002 path.
- [x] **GREEN:** enforce capacity only in Docker Executor: discover/claim only with a local slot, start no runtime before `200 claimed`, start none after `404`, `409 older_task_must_be_claimed_first`, `409 task_already_claimed`, or any other error, emit ordered `running` before runtime start and exactly one of `finished` or `failed`, never send `accepted_sequence`, verify every token team/project/task/environment/Executor/audience/expiry claim, free the slot only after terminal handling, use `tasks.resolved_image` from the claim response verbatim and refuse the task when the resolved image cannot be pulled or started; never substitute or maintain a local fallback image.
- [x] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./executor_docker_opehands/... -run 'TestTeamIdentity|TestRegistration|TestDiscovery|TestClaim|TestCapacity|TestEventForwarding|TestControl|TestEnvironment|TestResolvedImageAdopted|TestNoLocalImageFallback'`; require no cross-team data or runtime, no more than configured containers, no Router/Env Registry calls, no reliance on Registry capacity, and exact adoption of `tasks.resolved_image`.
  - **GREEN evidence:** the exact Section 10 Playwright command reports `13 passed`; the complete Executor race-enabled Go suite reports `122 passed` across 14 packages.
- [x] **REFACTOR:** simplify Executor client and state machine while retaining service-local boundaries and all one-team safeguards; rerun targeted and related tests.
  - **REFACTOR evidence:** the State Registry client and runtime path remain executor-local; the legacy mocked-task-server service, client, configuration, and tests were removed, and `StateRegistryURL` is mandatory; all Section 10 gates remain green.

## 11. Architecture diagrams

- [x] Verify the six v0002 source files retain their declared families: `state-registry-c4-container.puml` (`c4-container`), `state-registry-services-sequence.puml` (`sequence`), `state-registry-api-sequence.puml` (`sequence`), `state-registry-task-state-machine.puml` (`state-machine`, already authorized), `state-registry-executor-capacity-state-machine.puml` (`state-machine`, already authorized), and `state-registry-image-resolution-sequence.puml` (`sequence`). After the implementation changes are applied, re-run `~/config/ai/skills/puml-diagrams/puml-verify` for every changed diagram and report which diagrams require re-verification; do not add a new restricted diagram family.
  - **Evidence:** all six declarations match their authorized families. No diagram source required an implementation correction in this pass, so no file became newly stale; all six were still re-verified below.
- [x] Verify all six diagrams match immutable `team_id`, optional display-only `team_name`, one-team/one-tag Executor identity with `scope = team | scope = system`, the corrected cross-team source-system identity semantics (a source system belongs to one team) plus the globally unique `source_systems.listener_identity` index (one listener principal maps to exactly one `(team_id, source_system_id)`), `source_id` dedupe by `(team_id, source_system_id, source_id)`, admin-only team/source-system/task-type registration, required `teams.default_image`, REQUIRED non-null same-team `tasks.task_type_id` constrained by `(tasks.task_type_id, tasks.team_id) = (task_types.task_type_id, task_types.team_id)`, server-derived `tasks.required_tag`, integrity-protected cursors bound to endpoint, scope, filter tuple, ordering, and forward page direction, four-level image precedence (`tasks.image` -> `task_types.default_image` -> `source_systems.default_image` -> `teams.default_image`), the removed `dispatched` lifecycle state, `pending` projection before claim with no lifecycle event, the FIRST `created` event on successful claim with non-null `executor_id` and a payload meaning `task <task_id> loaded by <executor_id>`, the FIFO claim boundary replacing the never-accepted v0002-draft `/approvals` with `/claim`, `command_id` and `owner_command_id` semantics, scope-conditional team predicates, 3NF tables, `executor_events.executor_id NOT NULL`, strict `(occurred_at, event_id)` ordering, no `accepted_sequence`, non-revealing point `404`, collection `204`/empty semantics, image strings not carrying team authority, allow-listed `HS256`/`HS384`/`HS512` HMAC open-environment tokens under a server-controlled rotating `key_id` carried in the `X-FlowAI-Scope-Token` header with protected-header `kid` equal to payload `key_id`, lifetime bounded to five minutes, `expiry > issued_at`, `issued_at <= server_now + 30 seconds`, constant-time MAC comparison, the single non-revealing `404 environment_unknown_or_unavailable` shape across algorithm/`kid`/key/MAC/TTL/expiry/audience/claim/identity/terminal/not-assigned failures with zero decrypt operations, local AES-256-GCM authenticated encryption (256-bit data key, fresh 96-bit nonce per encryption, 128-bit authentication tag, associated-data binding team identifier, logical secret identifier, and version), opaque `key_id` / `key_version` envelope recorded on every `secret_versions` row, and fail-closed startup/decrypt behavior with no plaintext/key/nonce/ciphertext/tag internals in logs, audit entries, or error responses.
  - **Evidence:** the C4 overview and five focused flow/state diagrams collectively encode the implemented ownership, admin, ingestion, cursor, FIFO claim, event, capacity, image, HMAC-token, AES-GCM, non-revealing-error, and plaintext-exclusion contracts without introducing an ER diagram or another family.
- [x] Run `~/config/ai/skills/puml-diagrams/puml-verify <file> --checkonly` for every changed diagram; require every diagram to compile without adding an ER diagram or any unrequested diagram family.
  - **Evidence:** `puml-verify --checkonly` reports `[plantuml] OK` independently for all six v0002 sources.

## 12. Four-level image resolution and required team default

- [x] **RED E2E:** implement `v0002.50` (rewrite for required default image), `v0002.51` (rewrite for required default image), `v0002.52`, `v0002.53`, `v0002.74`, `v0002.75`, `v0002.76`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(50|51|52|53|74|75|76)\b'` and require failures for required `default_image` at registration, task-type default precedence, source-system default precedence, task override precedence, and claimed `resolved_image` + `image_source` persistence.
  - **RED evidence:** the exact seven-case command reported `5 passed, 2 failed`. Registry precedence cases `.50`, `.51`, `.74`, `.75`, and `.76` were already green; `.52` and `.53` failed at the real Docker boundary until the fixture used a schema-valid local digest and captured the pre-canonicalization Docker create request.
- [x] **RED unit/integration:** add table-driven tests for setting and immutability of `teams.default_image`; submitting, replacing, and immutability-locking of `tasks.image`; resolution precedence on claim (`tasks.image` -> `task_types.default_image` -> `source_systems.default_image` -> `teams.default_image`); the claim response containing `resolved_image` and `image_source` exactly when the resolved image is non-null; Executor using `resolved_image` verbatim with no local fallback; post-claim immutability of `resolved_image` even after `teams.default_image` changes via new-team registration; image equality across teams granting no authority.
  - **RED evidence:** schema, admin, ingestion, claim, precedence, and Executor tests pin the image contract at each boundary; the E2E RED above additionally proved that daemon-normalized inspect metadata cannot substitute for the exact create-request boundary.
- [x] **GREEN:** keep `POST /admin/teams` as the exclusive path to set `default_image`; persist `default_image` as opaque and non-null; never edit or clear through any admin or operator path. Accept `image` on `TaskIngestionRequest` and persist it on `tasks.image` in the same ingestion transaction; reject listener retries that change `image` on an already-seen `(team_id, source_system_id, source_id)`. Require non-null `tasks.task_type_id` and persist it on the canonical task; enforce the composite `(tasks.task_type_id, tasks.team_id) = (task_types.task_type_id, task_types.team_id)` foreign-key constraint so a foreign-team `task_type_id` cannot be cited. Derive `tasks.required_tag` server-side from the referenced `task_type.execution_tag`; reject any listener-supplied `required_tag` value as not authoritative. Resolve the effective image on atomic claim using `tasks.image` -> `task_types.default_image` -> `source_systems.default_image` -> `teams.default_image`, persist `tasks.resolved_image` and `tasks.image_source` in the same transaction that appends the first `created` event, and return both in `ClaimResponse` whenever the resolved image is non-null (which is always, because the team default is required).
- [x] **GREEN:** update the Docker Executor client to use `resolved_image` from the claim response verbatim when present, refuse the task when `resolved_image` cannot be pulled or started, record the four-level precedence source on the container metadata, and never substitute or maintain a local image; the Executor SHALL NOT pull, mutate, verify, or interpret the digest. Image strings SHALL NOT be used to grant team authority.
- [x] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./svc/state-registry/... ./executor_docker_opehands/... -run 'TestRequiredTeamDefaultImage|TestTaskImageOverride|TestTaskTypeDefaultImage|TestSourceSystemDefaultImage|TestImageResolutionPrecedence|TestClaimImageResolved|TestExecutorUsesResolvedImage|TestNoLocalImageFallback|TestImageEqualityNoCrossTeamAuthority'`; require post-claim immutability, exact image-by-source matching between claim response and Executor runtime, audit entries for every accepted admin write, no foreign team default image access, and zero image content in operator responses, logs, or audit entries.
  - **GREEN evidence:** the exact seven-case Playwright command reports `7 passed`; TypeScript reports `ok`; the checklist's historical Go regex matched no current names, so the corrected integration-tagged command covering `TestImageColumns`, `TestRequiredTeamDefaultImage`, `TestClaimRecordsResolvedImageAndSource`, `TestFourLevelImageResolutionPrecedence`, `TestIngestRetainsImageOnRetry`, `TestResolvedImagePersistedAtClaim`, `TestAdminRequiresDefaultImage`, `TestListenerIngestRetainsCanonicalImageOnRetry`, and `TestV0002ResolvedImageAdopted` reports `13 passed`.
- [x] **REFACTOR:** isolate image-column migration, image write/read handlers, image-resolution transaction logic, and Executor image-source selection; rerun all image, environment, and claim tests.
  - **REFACTOR evidence:** Registry image persistence/resolution and Executor adoption remain isolated; the E2E recorder observes only Docker create `Image` and labels, forwards original bytes unchanged, and avoids relying on daemon-normalized inspect metadata.

## 13. Executor ownership scope: team-owned versus system-owned

- [x] **RED E2E:** implement `v0002.54` through `v0002.59`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(54|55|56|57|58|59)\b'` and require failures for system-owned registration, system-owned cross-team discovery, team-owned isolation under system-owned visibility, system-owned cross-team claim, system-owned task-event envelope carrying the task's `team_id`, and scope-change rejection on re-registration.
  - **RED evidence:** the earlier registration/discovery RED slice reported `1 passed, 11 failed` while system scope returned unimplemented/unauthorized responses; the claim and lifecycle RED slices separately failed the cross-team claim and parent-task team-envelope cases before their handlers existed.
- [x] **RED unit/integration:** add table-driven tests for the `scope` discriminator: `scope = team` with valid existing `team_id` accepted; `scope = team` with null `team_id` rejected; `scope = team` with non-existent `team_id` rejected; `scope = system` with null `team_id` accepted; `scope = system` with non-null `team_id` rejected; re-registration with a different `scope` rejected without mutation; conditional team predicate in discovery (`team_id AND tag` for team-owned, `tag` only for system-owned) and FIFO ordering `(ingested_at ASC, task_id ASC)`; conditional claim team predicate and FIFO ordering; conditional task-event envelope `team_id` verification (`envelope team_id = executor team_id AND = task team_id` for team-owned, `envelope team_id = task team_id` for system-owned); conditional self-event envelope `team_id` verification (envelope non-null and equal to executor team for team-owned, envelope null for system-owned); audit entries recording `executor_scope`; Executor registration body never creates or updates a team.
  - **RED evidence:** handler and PostgreSQL suites were introduced against the earlier team-only surface and pinned nullability, immutable scope, conditional predicates, and event-envelope behavior before system scope became green.
- [x] **GREEN:** add the `scope` column to `executors`; allow `team_id` to be NULL on the row when `scope = system`; reject registrations where `scope = team` and `team_id` is null, `scope = system` and `team_id` is non-null, `scope = team` and `team_id` does not reference an existing team, or any registration that supplies more than one tag. Persist `scope` immutably from registration onward; reject a re-registration whose `scope` differs from the existing row. Apply the team predicate conditionally in discovery, claim, task-event envelope verification, and self-event envelope verification per the scope. Audit entries for every accepted task event and self event SHALL include `executor_scope`. The team predicate is omitted when `scope = system`, so a system-owned Executor MAY discover, claim, and emit events for any `pending` task with the matching tag from any team; the team predicate is required when `scope = team`, so a team-owned Executor's discovery, claim, and event writes are limited to same-team tasks. The parent task's `team_id` remains immutable for the row's lifetime regardless of Executor scope; the `NULL` `team_id` on a system-owned Executor never replaces the parent task's `team_id`.
- [x] **GREEN:** update the Docker Executor client to derive its `scope` from operator configuration at process startup; submit the same `scope` on every registration, re-registration, and self event; refuse to start work when its registered scope does not match its operator configuration; submit `team_id = null` and set envelope `team_id` to the parent task's `team_id` when `scope = system`; submit its immutable `team_id` and require envelope `team_id` to match when `scope = team`. Record `flowai.executor_scope` and the parent task's `flowai.team_id` on every owned container label; record `flowai.command_id` and `flowai.resolved_image_source` whenever applicable.
- [x] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./svc/state-registry/... ./executor_docker_opehands/... -run 'TestExecutorScopeDiscriminator|TestSystemExecutorRegistration|TestSystemExecutorCrossTeamDiscovery|TestTeamExecutorDiscoveryRemainsScoped|TestSystemExecutorCrossTeamClaim|TestSystemExecutorTaskEventEnvelope|TestExecutorScopeReRegistration|TestAuditRecordsExecutorScope'`; require exactly one team-owned Executor row per team per tag, exactly one system-owned Executor row per tag with NULL `team_id`, no cross-team data leak to team-owned Executors, scope-change rejection on re-registration, task-event envelope carrying the parent task's `team_id` from system-owned Executors, and audit entries with `executor_scope` on every accepted event.
  - **GREEN evidence:** the exact `.54`–`.59` Playwright command reports `6 passed`; the corrected integration-tagged scope suite covering actual registration, re-registration, conditional discovery, claim, event, and client tests reports `33 passed` across 25 packages.
- [x] **REFACTOR:** isolate the conditional team predicate in discovery, claim, and event-envelope verification; rerun all registration, discovery, claim, event, and image tests for both team-owned and system-owned Executors.
  - **REFACTOR evidence:** scope-specific predicates remain centralized in Registry repositories and event validation, while the self-contained Executor client derives and forwards scope without importing Registry internals.

## 14. Admin-only read-only projections under `/admin/tags` and `/admin/tasks`

- [x] **RED E2E:** implement `v0002.79`, `v0002.80`, and `v0002.81`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(79|80|81)\b'` and require behavior-specific failures for the system-admin-only tag projection, the system-admin-only global task projection, and identity enforcement plus non-mutation on both endpoints.
  - **RED evidence:** the three immutable cases were generated against the prior admin-create-only surface; tag/task reads were absent and therefore failed at the external route boundary before the projection handlers were implemented.
- [x] **RED unit/integration:** add tests for: non-admin identities (listener, team-owned Executor, system-owned Executor, API Gateway operator, anonymous) receive `401 unauthenticated` or `403 not_authorized` BEFORE any `task_types` or `tasks` row is read; an authenticated system administrator can list every `task_types` row ordered `(team_id ASC, execution_tag ASC, task_type_id ASC)` with cursor pagination default 50 and maximum 200 entries per response; an authenticated system administrator can list every canonical `tasks` row ordered `(ingested_at DESC, task_id DESC)` with cursor pagination default 50 and maximum 200 entries per response; both reads return a paginated page and SHALL NOT be unbounded; equal `execution_tag` strings appearing under different `team_id`s grant no cross-team authority; both reads never mutate any team, source system, task type, task, assignment, event, environment, secret, secret version, control, or audit row; both reads never return task payload values, decrypted environment values, secret material, nonce bytes, ciphertext bytes, authentication tag bytes, key material, or event history; out-of-range or malformed `limit` is rejected with `400` before any row is read; the optional exact-match filters `team_id`, `state`, `tag`, and `task_type_id` narrow the global collection; combined filters require all filters to match.
  - **RED evidence:** the recording-repository tests first observed malformed limits/cursors reaching protected queries and absent deterministic projection shaping; the generated E2E cases pinned identity rejection and full non-mutation at the real process boundary.
- [x] **GREEN:** implement `GET /admin/tags` and `GET /admin/tasks` accepting only an authenticated system-administrator identity. Both endpoints SHALL reject every non-admin identity before reading any row, perform no mutation, never return task payload values, decrypted environment values, secret material, or event history, and never grant authority from cross-team tag equality. There is no `tags` table, no tag create/update/delete API, no team update/delete API, and no system-admin authentication-mechanism definition.
- [x] **GREEN:** implement the `AdminTagEntry`, `AdminTagPage`, `AdminTaskEntry`, `AdminTaskPage`, and `AdminPageInfo` response schemas; the `AdminCursor`, `AdminLimit`, `AdminTagTeamIdFilter`, `AdminTagTagFilter`, `AdminTaskTeamIdFilter`, `AdminTaskStateFilter`, `AdminTaskTagFilter`, and `AdminTaskTypeIdFilter` query parameters; and the deterministic ordering and pagination rules. The tag page ordering SHALL be `(team_id ASC, execution_tag ASC, task_type_id ASC)`; the task page ordering SHALL be `(ingested_at DESC, task_id DESC)`.
- [x] **GREEN VERIFY:** rerun the targeted Playwright command and `go test ./svc/state-registry/... -run 'TestAdminListTags|TestAdminListTasks|TestAdminReadsRejectNonAdmin|TestAdminReadsAreNonMutating|TestAdminTagPagination|TestAdminTaskPagination|TestAdminTagFilter|TestAdminTaskFilter|TestAdminTagOrdering|TestAdminTaskOrdering|TestAdminReadsNeverExposePayload|TestAdminReadsNeverExposeSecrets|TestAdminReadsNeverExposeEvents|TestAdminReadsNeverExposeEnvironments|TestAdminLimitBounds'`; require system-admin-only enforcement on both reads; non-mutation of every team, source system, task type, task, assignment, event, environment, secret, control, and audit row; pagination defaults to 50 and caps at 200; ordering deterministic and stable across pages; no payload, decrypted environment value, secret material, or event history exposed; equal tag strings across teams grant no cross-team authority; out-of-range or malformed `limit` rejected with `400` before any row is read.
  - **GREEN evidence:** the exact `.79`–`.81` Playwright command reports `3 passed`; the implementation's consolidated cursor/shape suite replaces the checklist's obsolete test names and reports `61 passed` across 10 packages, covering reject-before-query cursor/limit validation, deterministic admin ordering, duplicate/oversized filters, task-type filtering, exact task allowlists, and non-admin rejection.
- [x] **REFACTOR:** isolate admin-read authorization, query construction, and response shaping from existing Gateway read paths; rerun targeted and related admin-read tests and confirm Gateway team isolation remains unchanged.
  - **REFACTOR evidence:** admin role authorization, cursor construction, repository queries, and response allowlists are separate from trusted-Gateway team reads; cross-scope cursor rejection remains green.

## 15. Final verification and OpenSpec gates

- [x] Fill every moved definition `v0002.1` through `v0002.81` with its exact Playwright file path and test title; verify no test is skipped and every implementation slice records valid behavior-specific RED-before-GREEN evidence.
  - **Evidence:** mechanical audit reports 81 definitions, ordinals `1..81` with no gaps, 81 unique runnable Playwright titles, zero duplicate IDs, zero skipped/fixme tests, and every `## Implementation reference` populated with its exact project-relative spec path and title.
- [x] Add new test-case definitions `v0002.60` through `v0002.81` to `autotest/test-cases/` at the start of this change's implementation; generate one runnable Playwright test per definition with title containing the immutable ID; require each test to fail for the expected behavior-specific reason before any production code is written for that slice and to pass after the GREEN step.
  - **Evidence:** definitions `.60` through `.81` are present in `autotest/test-cases/`, each maps one-to-one to a runnable title, and their RED/GREEN evidence is recorded in the corresponding implementation sections above.
- [x] Run `gofmt` on changed Go files, `go test -race ./...`, `go test -tags=integration ./svc/state-registry/...`, and `go build ./...`; require unit, integration, race, and build checks to pass.
  - **Evidence:** changed Go files were formatted; repository-wide race reports `649 passed` across 32 packages; tagged State Registry integration reports `657 passed` across 11 packages; `go vet ./...` and `go build ./...` succeed.
- [x] Run `npm --prefix autotest/state-registry test` and the related Docker Executor suite; require all 81 v0002 E2E tests (1-81) to pass, including concurrent FIFO claim, idempotent same-command retry, one command owning multiple tasks, FIFO `409 older_task_must_be_claimed_first`, admin-only team creation with required `default_image`, four-level image precedence, shared image across teams without authority, source_id dedupe with corrected cross-team independence, AES-256-GCM fail-closed startup, AES-256-GCM successful open, AES-256-GCM denial without any decrypt operation, system-admin-only `GET /admin/tags` and `GET /admin/tasks` projections, and identity enforcement plus non-mutation on both admin reads.
  - **Evidence:** the complete State Registry Playwright suite reports `137 passed`, including all 81 immutable `v0002.1` through `v0002.81` cases and the normal untagged production mTLS route test; the related Docker Executor Playwright suite reports `11 passed` after the State Registry suite completed.
- [x] Run strict change validation: `npx -y @fission-ai/openspec@1.5.0 validate v0002-state-registry --strict`.
  - **Evidence:** strict change validation reports `Change 'v0002-state-registry' is valid`.
- [x] Run full strict validation: `npx -y @fission-ai/openspec@1.5.0 validate --all --strict`.
  - **Evidence:** full strict validation reports `6 passed, 0 failed`.
- [x] Confirm the change remains active and unarchived until implementation, verification, acceptance, and artifact sync are complete.
  - **Evidence:** the implemented change remains at `openspec/changes/v0002-state-registry/`; it has not been moved under `openspec/changes/archive/` and is ready for explicit acceptance/sync/archive workflow.
