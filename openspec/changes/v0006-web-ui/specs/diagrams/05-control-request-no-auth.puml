@startuml
title v0006-web-ui - control request without auth

participant "Web UI" as UI
participant "API Gateway" as GW
participant "State Registry" as State

UI -> GW : inject / cancel / rerun request
GW -> State : POST control-request record
State --> GW : accepted
GW --> UI : accepted

@enduml
