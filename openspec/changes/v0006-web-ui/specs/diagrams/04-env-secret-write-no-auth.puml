@startuml
title v0006-web-ui - env and secret write without auth

participant "Web UI" as UI
participant "API Gateway" as GW
participant "Env Registry" as Env

UI -> GW : submit env vars and secret values
GW -> Env : proxy store request
Env --> GW : env ids and secret ids
GW --> UI : response unchanged

@enduml
