# Tasks

Implementation SHALL proceed in the order below. No GREEN production edit may begin until its corresponding runnable E2E test has failed for the expected behavior-specific reason. Every slice SHALL record the exact RED command and expected failure, the exact GREEN command and pass, and the related-suite result before its implementation task is checked. REFACTOR may run only while the targeted and related tests remain green.

## 1. Implementation-start test activation and API-only harness

- [ ] **RED PREPARATION:** preflight destinations, then move `specs/test-cases/v0002.1-*.md` through `v0002.45-*.md` unchanged into `autotest/test-cases/` as the first implementation mutation; verify all 45 immutable IDs are contiguous, no destination collides, and no v0002 definition remains under the change folder.
- [ ] **RED:** bootstrap `autotest/state-registry/` as an API-only Playwright suite that starts real State Registry, PostgreSQL, and OpenBao processes; provides distinct listener, Executor, and trusted API Gateway mTLS identities bound to `team-a` or `team-b`; supports controlled Registry restarts; captures OpenBao calls; and can open concurrent WebSockets. Verify a smoke test fails only because the protected State Registry behavior is not implemented, not because fixtures, certificates, or dependencies are broken.
- [ ] **GREEN:** add one runnable Playwright test for every moved definition `v0002.1` through `v0002.45`; each title SHALL contain its immutable ID, every request SHALL use a supported external HTTP/WebSocket interface, and no test SHALL be skipped or replaced by an in-process assertion.
- [ ] **GREEN VERIFY:** run `npm --prefix autotest/state-registry test -- --list` and require exactly one discoverable runnable test per `v0002.<ordinal>` from 1 through 45 with no gaps or duplicates; leave each definition's `## Implementation reference` blank until its exact file path and title are stable.
- [ ] **REFACTOR:** centralize only team fixtures, transport identities, process lifecycle, HTTP/WebSocket clients, and response-shape assertions; rerun the harness smoke/list checks and require behavior assertions to remain explicit in each test.

## 2. Normalized team-owned PostgreSQL persistence

- [ ] **RED (migration/integration):** add tagged PostgreSQL tests that fail because the exact 3NF team-owned schema, constraints, and foreign keys do not exist; the failure SHALL name missing tables or ownership constraints rather than an unavailable database fixture.
- [ ] **GREEN:** add versioned State Registry migrations for exactly `teams`, `executors`, `tasks`, `task_events`, `executor_events`, `environment_definitions`, `secrets`, `secret_versions`, `audit_entries`, and `task_control_requests`. `teams` SHALL own immutable `team_id` and display-only `team_name`; authorization SHALL NOT depend on `team_name`.
- [ ] **GREEN:** enforce 3NF relationships `teams 1:N executors`, `teams 1:N tasks`, `teams 1:N environment_definitions`, `teams 1:N secrets`, `executors 1:N executor_events`, `executors 1:N tasks`, `tasks 1:N task_events`, `tasks 1:N task_control_requests`, `environment_definitions 1:N secrets`, and `secrets 1:N secret_versions`; child rows SHALL inherit and database-enforce the same team through composite foreign keys or equivalent constraints.
- [ ] **GREEN:** enforce uniqueness on `(team_id, source, source_task_id)` and `(task_id, event_id)`; exactly one non-empty task tag and Executor tag; nullable `tasks.executor_id` before dispatch and immutable same-team assignment after dispatch; `task_events.executor_id = NULL` only for Registry-owned `created`/`dispatched`; non-null assigned `task_events.executor_id` for Executor lifecycle events; and `executor_events.executor_id NOT NULL` for every self event. `secret_versions` SHALL reference logical `secrets` rows.
- [ ] **GREEN VERIFY:** run `go test -tags=integration ./state-registry/... -run 'TestMigrations|TestTeamOwnedSchema|TestNormalizedRelationships|TestPersistenceConstraints'`; require the exact table set, 3NF placement, immutable team ownership, child-team rejection, nullability, uniqueness, same-team foreign keys, secret/version relationship, and rollback behavior to pass. `v0002.32` and `v0002.44` cover externally observable persistence and display-name authority; migration tests remain the structural 3NF check.
- [ ] **REFACTOR:** consolidate migration fixtures and SQL assertions without hiding table/constraint names or weakening exact-schema checks; rerun the same tagged command.

## 3. Team-bound listener ingestion and deduplication

- [ ] **RED E2E:** implement `v0002.1`, `v0002.2`, `v0002.30`, `v0002.32`, and `v0002.33`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(1|2|30|32|33)\b'` and require behavior-specific failures for listener team authorization, transactional `created`, post-commit visibility, `(team_id, source, source_task_id)` deduplication, cross-team independence, or restart durability.
- [ ] **RED unit/integration:** add tests for listener identity/team mismatch, concurrent same-team retries, identical source identities across teams, rollback between task and `created`, and discoverability only after commit; require failures before repository and transaction code exists.
- [ ] **GREEN:** implement listener ingestion with a service identity bound to one immutable authorized `team_id`; verify the submitted team, insert the complete team-owned canonical task and Registry-owned `created` event in one transaction, acknowledge only after commit, and return the existing same-team task on `(team_id, source, source_task_id)` retry without resetting state or history.
- [ ] **GREEN VERIFY:** rerun the targeted Playwright command and `go test ./state-registry/... -run 'TestListenerTeamBinding|TestIngest|TestTeamScopedDeduplicate|TestCommitVisibility'`; require unauthorized-team submissions to create nothing, same-team retries to append no duplicate, different teams to receive different tasks for the same source identity, and restart reads to preserve ownership.
- [ ] **REFACTOR:** simplify ingestion/repository boundaries while preserving one transaction and explicit team predicates; rerun targeted and related ingestion tests.

## 4. One-team/one-tag Executor registration and isolated discovery

- [ ] **RED E2E:** implement `v0002.3`, `v0002.12`, `v0002.14`, `v0002.16`, `v0002.20`, `v0002.27`, `v0002.28`, and `v0002.34`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(3|12|14|16|20|27|28|34)\b'` and require failures for team-bound identity, exactly-one-tag registration, same-team exact-tag discovery, foreign-task collection isolation, mTLS, or capacity-independent discovery.
- [ ] **RED unit/integration:** add table-driven registration tests for omitted/changed/foreign team and zero/one/multiple tags; add discovery query tests for `team_id AND required_tag`, unregistered tags, team filtering before pagination/counts/cursors, read-only behavior, and absence of capacity reads.
- [ ] **GREEN:** implement Executor identity binding to one immutable `team_id`; registration SHALL persist that team, one authorized tag, type, metadata, and observed `max_capacity`/`running_count`, rejecting omitted or changed teams and invalid tag counts without persistence.
- [ ] **GREEN:** implement read-only discovery of only `created` tasks where both `tasks.team_id = authenticated_executor.team_id` and `required_tag = registered_tag`; apply both predicates before result shaping; return `204` or an empty collection when only foreign tasks match; reveal no foreign count, cursor, page metadata, or empty/non-empty distinction; never reserve, assign, or evaluate capacity.
- [ ] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./state-registry/... -run 'TestExecutorTeamRegistration|TestRegistrationTagCount|TestTeamTagDiscovery|TestDiscoveryShape|TestDiscoveryIgnoresCapacity'`; require immutable one-team/one-tag records, non-revealing foreign collection behavior, unchanged task state, and no capacity-based result.
- [ ] **REFACTOR:** isolate registration validation and team/tag query construction without allowing caller-selected tenant predicates; rerun all registration/discovery tests.

## 5. Atomic same-team approval and immutable assignment

- [ ] **RED E2E:** implement `v0002.4`, `v0002.21`, `v0002.22`, `v0002.28`, and `v0002.35`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(4|21|22|28|35)\b'` and require failures for same-team compare-and-set approval, stale conflicts, discovery removal, capacity independence, or foreign point-resource `404`.
- [ ] **RED unit/integration:** add concurrent approval and rollback tests for same-team same-tag eligibility, tag mismatch, team mismatch, saturated observations, immutable assignment, one `dispatched` row, and identical foreign/unknown not-found translation.
- [ ] **GREEN:** implement one approval transaction requiring `created`, exact registered tag, and equal immutable task/Executor `team_id`; atomically append Registry-owned `dispatched` with `executor_id = NULL`, set immutable same-team `tasks.executor_id`, record `approved_at`, update projection, and remove the task from same-team discovery.
- [ ] **GREEN:** return exactly one `200 approved` winner and `409 task_already_dispatched` to later eligible same-team competitors; return the unknown-resource `404` shape for a foreign `task_id` with no mutation or event; do not read capacity and never return `422 executor_at_capacity`.
- [ ] **GREEN VERIFY:** rerun targeted Playwright tests and `go test -tags=integration ./state-registry/... -run 'TestConcurrentTeamApproval|TestApprovalTransaction|TestForeignApprovalNotFound|TestApprovalIgnoresCapacity'`; require one immutable assignee, one dispatched event, no cross-team signal, and no capacity gate.
- [ ] **REFACTOR:** extract approval eligibility and error translation without splitting assignment, event append, projection, and discovery removal across transactions; rerun all approval tests.

## 6. Team-scoped lifecycle events, self events, projection, and strict ordering

- [ ] **RED E2E:** implement `v0002.5`, `v0002.6`, `v0002.23` through `v0002.26`, `v0002.29`, `v0002.31`, and `v0002.36`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(5|6|23|24|25|26|29|31|36)\b'` and require failures for assignment ownership, team isolation, event/self-event separation, strict ordering, idempotency, invalid transitions, foreign point `404`, or restart recovery.
- [ ] **RED unit:** add table-driven transition tests for `created -> dispatched -> running -> finished | failed`, running-before-dispatch, terminal-before-running, repeated terminal, re-approval, unassigned same-team writer, foreign-team writer, identical retry, equal timestamp with greater/lower `event_id`, older tuple, and caller-supplied `accepted_sequence`; require behavior-specific failures before validators exist.
- [ ] **RED integration:** force failures between event append and projection update and between `executor_events` append and observation mirroring; require no partial commit and test database rejection of foreign-team child ownership and null Executor ownership.
- [ ] **GREEN:** accept `running`, `finished`, and `failed` only from authenticated `tasks.executor_id` with the same immutable team; require `event_id`, `task_id`, non-null `executor_id`, explicit envelope `team_id`, `event_type`, `occurred_at`, and `payload`; verify the envelope `team_id` equals the authenticated Executor team; append once by `(task_id, event_id)` and project state in the same transaction. Foreign task or Executor point identifiers SHALL return unknown-shape `404`; unassigned same-team writers SHALL append nothing; envelope `team_id` mismatches SHALL be rejected without append.
- [ ] **GREEN:** resolve an identical `event_id` retry before ordering checks; require each fresh `(occurred_at, event_id)` tuple to be strictly greater than the latest accepted tuple; read by `(occurred_at ASC, event_id ASC)`; allow equal timestamps only with greater monotonic `event_id`; reject `accepted_sequence` and every recovery override without append or projection change. Registry-owned `created` and `dispatched` rows SHALL retain `task_events.executor_id = NULL` and inherit `team_id` from the task row.
- [ ] **GREEN:** append Executor lifecycle/health/capacity events separately with `executor_events.executor_id NOT NULL`, envelope `team_id` equal to the authenticated Executor team, inherited same-team ownership, and same-transaction mirroring of latest observations; never use those observations in discovery, approval, or lifecycle decisions.
- [ ] **GREEN VERIFY:** rerun the targeted Playwright command, `go test ./state-registry/... -run 'TestTransition|TestTeamEventOwnership|TestEventIdempotency|TestStrictEventOrdering|TestExecutorSelfEvent'`, and tagged transaction tests; require exact ordered histories, unchanged state on every rejection, no duplicate identifiers, no accepted sequence, and no cross-team append.
- [ ] **REFACTOR:** consolidate transition, envelope, ownership, and ordering validation while preserving distinct task/self streams and transaction boundaries; rerun all lifecycle tests.

## 7. Trusted Gateway context, team-filtered reads, controls, audit, and WebSocket

- [ ] **RED E2E:** implement `v0002.7`, `v0002.12`, `v0002.37`, `v0002.41`, `v0002.42`, `v0002.43`, and `v0002.44`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(7|12|37|41|42|43|44)\b'` and require failures for trusted Gateway enforcement, operator team checking, controls, pre-shape filtering, plaintext-free audit, WebSocket isolation, or display-name non-authority.
- [ ] **RED unit/integration:** add tests that accept only authenticated Gateway service identity plus verified `operator_id`, immutable `team_id`, and `request_id`; allow the optional display-only `team_name` to be present or absent without affecting authorization; reject copied direct headers, invalid/multi-team operator context, foreign point identifiers, and team filters applied after pagination/count/cursor/aggregation. Add concurrent replay/live WebSocket isolation tests.
- [ ] **GREEN:** implement Gateway-only reads, subscriptions, controls, environment/secret writes, and audits. State Registry SHALL authorize solely by canonical `team_id`; the optional `team_name` SHALL remain display/audit metadata when present and its absence SHALL NOT alter authority, and changing or spoofing it SHALL NOT alter authority. Untrusted headers or payload tenant fields SHALL grant nothing.
- [ ] **GREEN:** apply `team_id` before pagination, cursor creation, totals, counts, grouping, aggregation, serialization, replay selection, subscription filters, live fan-out, and frame serialization for tasks, Executors, events, controls, environments, secrets, versions, and audits. Every foreign point identifier SHALL use the unknown-resource `404` shape; no collection metadata or WebSocket frame SHALL signal foreign rows.
- [ ] **GREEN:** store same-team controls idempotently by `(team_id, task_id, operator_id, action)` with plaintext-free audit entries containing team, actor, action, resource, request, outcome, and timestamp; expose pending controls only to the assigned same-team Executor.
- [ ] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./state-registry/... -run 'TestTrustedGatewayContext|TestForeignResourceNotFound|TestTeamFilterBeforeShape|TestTeamControl|TestTeamAudit|TestWebSocketTeamIsolation|TestTeamNameDisplayOnly'`; require response equivalence with foreign rows removed and zero cross-team frame, count, cursor, audit, timing, or existence signal.
- [ ] **REFACTOR:** centralize trusted context parsing, mandatory team predicates, non-revealing not-found translation, and WebSocket binding without permitting optional tenant filters; rerun all Gateway/read/control/subscription tests.

## 8. Team-owned environments, logical secrets, immutable versions, and open

- [ ] **RED E2E:** implement `v0002.8` through `v0002.11`, `v0002.19`, `v0002.32`, `v0002.38` through `v0002.40`, and `v0002.45`; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(8|9|10|11|19|32|38|39|40|45)\b'` and require failures for team-owned environment/secret reads or writes, logical secret versions, non-revealing foreign identifiers, complete token claims, scope-token algorithm/key_id/issued_at/expiry checks, terminal/unassignment replay denial, assignment, audit, or restart durability.
- [ ] **RED unit/integration:** add tests for same-team environment CRUD, project/task applicability within a team, foreign environment/secret/version point resources, no OpenBao call before authorization, logical `secrets` plus immutable referenced `secret_versions`, plaintext exclusion, Transit rotation, bounded retries, outages, and fail-closed open/write behavior.
- [ ] **GREEN:** implement environment-definition writes only from trusted Gateway context for the same immutable `team_id`, storing non-secret values and optional project/task applicability plus plaintext-free audit. Operators SHALL register and manage environments only for their team; scopes SHALL narrow use inside the team and never grant cross-team authority.
- [ ] **GREEN:** implement same-team logical `secrets` rows associated with same-team environments and append immutable encrypted `secret_versions` rows referencing them. Verify all team and scope ownership before OpenBao encryption; never persist, log, audit, or return plaintext; foreign environment, secret, or version identifiers SHALL return unknown-shape `404` and perform no OpenBao operation.
- [ ] **GREEN:** implement `GET /v1/environments/{environment_id}/open` with the compact three-part signed scope token `<header>.<payload>.<signature>` carried in the `X-FlowAI-Scope-Token` header only; no scope-token fields appear in the request body, and the `task_id` query parameter is required. State Registry SHALL be the sole issuer and verifier of these tokens, signing them with an allow-listed HMAC algorithm from the set `HS256`/`HS384`/`HS512` (the "HMAC-SHA-256 or a stronger HMAC" family) under a server-controlled rotating key identified by a `key_id` claim inside the documented active key window. Each valid token SHALL carry non-null `team_id`, `task_id`, `environment_id`, `executor_id`, `audience` (literal `state-registry.environment.open`), `key_id`, `issued_at`, and `expiry`, and the required `project_id` claim whose value is null only when the canonical environment has no project scope; the protected header SHALL carry `alg` (allow-listed), `kid`, and `typ`, and the protected-header `kid` SHALL equal the payload `key_id`; `issued_at <= server_now + 30 seconds`, `expiry > issued_at`, and `expiry - issued_at <= 5 minutes`. Verify the protected-header `kid` equals the payload `key_id` before any MAC computation, recompute the signature under the declared allow-listed HMAC algorithm and compare it with a constant-time comparison before any other check, then canonical claim shape and values (including the project-scope rule for `project_id`), `key_id` window, `issued_at`/`expiry` window, expected literal `audience`, every claim, the authenticated Executor team, same-team assignment, project/task applicability, and non-terminal task state before any OpenBao call. Reject every invalid or unavailable open attempt — including missing or empty token, tampered MAC, algorithm outside the allow-listed HMAC set, protected-header `kid` not equal to payload `key_id`, `key_id` outside the active window, `expiry` not strictly later than `issued_at`, `expiry` more than five minutes after `issued_at`, expired or premature-beyond-skew tokens, audience or canonical-claim mismatches (including a null `project_id` for a project-scoped environment), mismatched `team_id`, terminal task state, and any not-assigned caller — by returning the same non-revealing `404 environment_unknown_or_unavailable` shape with zero OpenBao calls and no token plaintext, individual claim values beyond identifier-level metadata, MAC bytes, key material, or derived key bytes in logs, audit entries, or error responses. Allow a same assigned identity retry within TTL only when every check still passes; the retry SHALL NOT bypass canonical claim, transition, or assignment checks, SHALL NOT extend TTL, and SHALL NOT revive an expired token. On success, return authorized values in memory only and append a plaintext-free team audit.
- [ ] **GREEN VERIFY:** rerun targeted Playwright tests and `go test -tags=integration ./state-registry/... -run 'TestTeamEnvironment|TestTeamSecret|TestSecretVersions|TestOpenEnvironmentClaims|TestScopeTokenSecurity|TestOpenBao|TestPlaintextExclusion'`; require same-team success, point-resource `404 environment_unknown_or_unavailable` parity for every invalid or unavailable variant, zero cryptography on denied writes/opens, immutable versions, rotation compatibility, fail-closed outages, constant-time MAC comparison, server-controlled `key_id` rotation, lifetime bounded to five minutes, and zero OpenBao calls for every invalid or unavailable variant including terminal and not-assigned cases.
- [ ] **REFACTOR:** separate team authorization, audit, scope-token verification, and Transit concerns behind narrow State Registry-owned interfaces without sharing code between services; rerun all environment/secret/open tests.

## 9. Docker Executor client, team identity, and local capacity ownership

- [ ] **RED E2E:** implement `v0002.13` through `v0002.19` against the finalized team contract; run `npm --prefix autotest/state-registry test -- --grep 'v0002\.(13|14|15|16|17|18|19)\b'` and require failures because the Docker Executor lacks one-team registration, same-team discovery/approval, strict event ordering, foreign `404` handling, complete scope-token checks, or local-only capacity enforcement.
- [ ] **RED unit/integration:** add Docker Executor tests for one immutable team + one tag, local slot gating, same-team exact-tag discovery, approval-before-start, `409` recovery, foreign/unknown `404` equivalence, non-null event ownership, strictly increasing ordering tuples, no `accepted_sequence`, stable retries, same-team controls, all scope-token claims, display-name non-authority, and terminal slot release.
- [ ] **GREEN:** update the self-contained Docker Executor State Registry client and OpenAPI documents for one-team registration, same-team exact-tag discovery, approval, task/self events, assigned controls, and team-bound open-environment reads. Every Executor-emitted task and self event envelope SHALL include `task_id` (when task-scoped), `executor_id`, explicit `team_id` equal to the authenticated Executor team, `event_id`, `event_type`, `occurred_at`, and `payload`. Remove Router and Env Registry client use from the v0002 path.
- [ ] **GREEN:** enforce capacity only in Docker Executor: discover/request approval only with a local slot, start no runtime before `200 approved`, start none after `404`, `409`, or any error, emit ordered `running` before runtime start and one terminal event, never send `accepted_sequence`, verify every token team/project/task/environment/Executor/audience/expiry claim, and free the slot only after terminal handling.
- [ ] **GREEN VERIFY:** rerun targeted Playwright tests and `go test ./executor-docker/... -run 'TestTeamIdentity|TestRegistration|TestDiscovery|TestApproval|TestCapacity|TestEventForwarding|TestControl|TestEnvironment'`; require no cross-team data or runtime, no more than configured containers, no Router/Env Registry calls, and no reliance on Registry capacity.
- [ ] **REFACTOR:** simplify Executor client and state machine while retaining service-local boundaries and all one-team safeguards; rerun targeted and related tests.

## 10. Architecture diagrams

- [ ] Verify the five existing v0002 sources remain the only changed diagram files and retain their declared families: `state-registry-c4-container.puml` (`c4-container`), `state-registry-services-sequence.puml` (`sequence`), `state-registry-api-sequence.puml` (`sequence`), `state-registry-task-state-machine.puml` (`state-machine`), and `state-registry-executor-capacity-state-machine.puml` (`state-machine`, already authorized by the proposal).
- [ ] Verify all five diagrams match immutable `team_id`, optional display-only `team_name`, one-team/one-tag Executor identity, team-scoped dedupe/discovery/approval/events/controls/environments/secrets/open, trusted Gateway context, pre-shape filtering/WebSockets, exact 3NF tables, non-null `executor_events.executor_id`, strict `(occurred_at,event_id)` ordering, no `accepted_sequence`, non-revealing point `404`, collection `204`/empty semantics, allow-listed `HS256`/`HS384`/`HS512` HMAC open-environment tokens under a server-controlled rotating `key_id` carried in the `X-FlowAI-Scope-Token` header with protected-header `kid` equal to payload `key_id`, lifetime bounded to five minutes, `expiry > issued_at`, `issued_at <= server_now + 30 seconds`, constant-time MAC comparison, and the single non-revealing `404 environment_unknown_or_unavailable` shape across algorithm/`kid`/key/MAC/TTL/expiry/audience/claim/identity/terminal/not-assigned failures with zero OpenBao calls.
- [ ] Run `plantuml -checkonly openspec/changes/v0002-state-registry/specs/diagrams/*.puml` and require all five sources to compile without adding an ER diagram or any unrequested diagram family.

## 11. Final verification and OpenSpec gates

- [ ] Fill every moved definition `v0002.1` through `v0002.45` with its exact Playwright file path and test title; verify no test is skipped and every implementation slice records valid behavior-specific RED-before-GREEN evidence.
- [ ] Run `gofmt` on changed Go files, `go test -race ./...`, `go test -tags=integration ./state-registry/...`, and `go build ./...`; require unit, integration, race, and build checks to pass.
- [ ] Run `npm --prefix autotest/state-registry test` and the related Docker Executor suite; require all 45 v0002 E2E tests and existing related E2E tests to pass, including concurrent cross-team WebSocket isolation and scope-token rejection without OpenBao.
- [ ] Run strict change validation: `npx -y @fission-ai/openspec@1.5.0 validate v0002-state-registry --strict`.
- [ ] Run full strict validation: `npx -y @fission-ai/openspec@1.5.0 validate --all --strict`.
- [ ] Confirm the change remains active and unarchived until implementation, verification, acceptance, and artifact sync are complete.
