# Autotest runtime artifacts (regenerated on every `npm test`).
playwright-report/
test-results/

# node_modules is already in the root .gitignore; this file is the local
# fallback in case the autotest subtree is ever vendored standalone.
node_modules/

# All generated QA reports and recordings are local artifacts.
reports/
