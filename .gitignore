# OpenSpec contract artifacts are tracked.
!openspec/
!openspec/config.yaml
!openspec/project.md
!openspec/AGENTS.md
!openspec/specs/
!openspec/specs/**
!openspec/changes/
!openspec/changes/**

# OpenCode surface (skills, commands, package) is environment-local.
# The canonical openspec skill is symlinked from ~/config/ai/skills/openspec/
# via .opencode/skills/openspec and resolves to the single source of truth.
.opencode/

# Local agent skill symlinks; canonical skills live outside this repo.
.agents/skills

# Local OpenSpec CLI/runtime cache. Persist only openspec/ source artifacts.
.tools/openspec
openspec/.cache/
openspec/tmp/
openspec/temp/

# Per-diagram rendered artifacts left by `puml-verify` without `--png`.
# Previews belong only under `.preview/` (already gitignored). The
# puml source under openspec/changes/*/specs/diagrams/ is the contract;
# the rendered .svg/.png siblings are regenerated on demand.
openspec/changes/**/specs/diagrams/*.svg
openspec/changes/**/specs/diagrams/*.png
docs/architecture/diagrams/*.svg
docs/architecture/diagrams/*.png

# Local environment and secret files. Commit examples/templates instead.
.env
.env.*
!.env.example
!.env.*.example

# Package-manager/runtime artifacts that may appear while using npx/npm.
node_modules/
.npm/
.pnpm-store/
.yarn/cache/

# Local editor and automation state.
.idea
.vscode
.omo/run-continuation
.preview/
