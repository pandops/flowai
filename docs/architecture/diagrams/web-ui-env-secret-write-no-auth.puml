' diagram-type: sequence
@startuml
title v0006 Web UI — launch parameter mutation
actor Operator
participant "Web UI" as UI
participant "Mocked proxy" as Proxy
database "State Registry" as Registry
Operator -> UI: enter key + value; submit
UI -> Proxy: mutation under /teams/{selected}/launch-parameters
Proxy -> Registry: forward exact selected team, key, value
Registry -> Registry: validate, persist revision/audit
Registry --> Proxy: safe metadata (no secret plaintext)
Proxy --> UI: unchanged response
UI -> Proxy: reload selected-team projection/history
Proxy -> Registry: canonical reads
Registry --> UI: active values + plaintext-free history
@enduml
