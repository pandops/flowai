# FlowAI Platform — Current Architecture

> **Status:** No runtime services are implemented or accepted yet.

`docs/architecture/` describes how the implemented current system is designed.
Because the current accepted system has no runtime capabilities yet, there is no
current service topology to document here.

Planned architecture lives in active numbered OpenSpec changes:

- `openspec/changes/0001-executor-docker/`
- `openspec/changes/0002-state-registry/`
- `openspec/changes/0003-env-registry/`
- `openspec/changes/0004-router/`
- `openspec/changes/0005-executor-k8s/`
- `openspec/changes/0006-web-ui/`
- `openspec/changes/0007-auth/`

After a change is implemented, verified, accepted, and synced into
`openspec/specs/`, copy or redraw the relevant implemented `.puml` diagrams into
`docs/architecture/diagrams/` and update this document to describe the current
state.
